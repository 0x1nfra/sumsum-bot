"""Weather edge-calculation boundary for later signal work.

Phase 01 preserves the documented module path only. Signal math is introduced
after market discovery foundations are complete.
"""

from __future__ import annotations


class WeatherEdgeCalculator:
    """Placeholder interface for future NOAA-versus-market edge calculations."""

    def calculate(self, *_args: object, **_kwargs: object) -> None:
        """Reject Phase 1 usage until weather signal logic is implemented."""
        raise NotImplementedError("Weather edge calculation is out of scope in Phase 1.")
