"""Sports scanner boundary for later strategy work.

Phase 01 keeps the documented package path in place without adding live sports
behavior.
"""

from __future__ import annotations


class SportsMarketScanner:
    """Placeholder sports scanner interface for future implementation."""

    def scan(self, *_args: object, **_kwargs: object) -> None:
        """Reject Phase 1 usage until sports scanning is implemented."""
        raise NotImplementedError("Sports market scanning is out of scope in Phase 1.")
