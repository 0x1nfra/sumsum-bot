---
phase: 04-paper-trading-runtime
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - paper_trader.py
  - main.py
  - core/storage.py
  - core/paper_runtime.py
  - core/paper_execution.py
  - core/clob_client.py
  - tests/integration/test_paper_runtime.py
autonomous: true
requirements: [PAPR-01, PAPR-02]
must_haves:
  truths:
    - "The operator can run a dedicated paper-trading entrypoint without introducing any live-order execution path."
    - "The runtime reuses the existing scan, signal, and risk pipeline rather than bypassing or duplicating it."
    - "Open positions survive loop iterations and process restarts because the runtime restores them from SQLite."
    - "Settlement uses an explicit resolver matrix and fails closed when a Polymarket market is not yet in a trustworthy terminal state."
  artifacts:
    - path: "paper_trader.py"
      provides: "Operator-facing paper runtime CLI"
    - path: "core/paper_runtime.py"
      provides: "Ordered runtime passes for scan, signal, risk, entry, resolution, and persistence"
    - path: "tests/integration/test_paper_runtime.py"
      provides: "Once-mode end-to-end verification over real storage and stubbed external dependencies"
  key_links:
    - from: "paper_trader.py"
      to: "core/paper_runtime.py"
      via: "thin CLI delegates to reusable runtime service"
      pattern: "run-paper-once|run-paper-loop|PaperRuntime"
    - from: "core/paper_runtime.py"
      to: "strategies/weather/signal_engine.py"
      via: "candidate scan and risk gate feed paper entry"
      pattern: "SignalEngine|evaluate_candidates|evaluate_risk_for_signal"
    - from: "core/paper_runtime.py"
      to: "core/storage.py"
      via: "restore open positions and persist runtime outputs"
      pattern: "list_open_paper_positions|persist_paper_position"
---

<objective>
Build the actual Phase 4 paper-trading runtime: a thin CLI and reusable backend loop that reuses the existing scan, signal, and risk pipeline, enters allowed trades immediately in paper mode, restores open positions from storage, and resolves them with an explicit fail-closed settlement matrix.

Purpose: satisfy PAPR-01 and the runtime half of PAPR-02 without coupling the bot to dashboard or deployment work.
Output: a paper-runtime service, a real operator-facing CLI, a concrete settlement resolver matrix, and passing integration coverage for once-mode execution and restart-safe open-position restoration.
</objective>

<execution_context>
@/Users/irfanmurad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-paper-trading-runtime/04-CONTEXT.md
@.planning/phases/04-paper-trading-runtime/04-RESEARCH.md
@.planning/phases/04-paper-trading-runtime/04-VALIDATION.md
@.planning/phases/04-paper-trading-runtime/04-PATTERNS.md
@paper_trader.py
@main.py
@core/clob_client.py
@core/storage.py
@core/paper_execution.py
@strategies/weather/signal_engine.py
@tests/integration/test_signal_risk_gate.py

<interfaces>
From paper_trader.py:
```python
def main(argv: Sequence[str] | None = None) -> int:
    ...
```

From core/storage.py after 04-01:
```python
class CandidateStorage:
    def list_open_paper_positions(self) -> list[PaperPositionRecord]:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red-first runtime integration coverage for once-mode paper trading and restart-safe restoration</name>
  <read_first>
    - paper_trader.py
    - main.py
    - core/storage.py
    - core/paper_execution.py
    - strategies/weather/signal_engine.py
    - tests/integration/test_signal_risk_gate.py
    - .planning/phases/04-paper-trading-runtime/04-RESEARCH.md
  </read_first>
  <files>tests/integration/test_paper_runtime.py</files>
  <acceptance_criteria>
    - `rg -n "test_paper_runtime_once_mode_enters_allowed_trade_without_live_execution" tests/integration/test_paper_runtime.py` returns one match.
    - `rg -n "test_paper_runtime_restores_open_positions_before_resolution_pass" tests/integration/test_paper_runtime.py` returns one match.
    - `rg -n "test_paper_runtime_skips_unresolved_markets_until_resolver_matrix_marks_terminal" tests/integration/test_paper_runtime.py` returns one match.
    - `rg -n "paper-once|paper-loop|resolver_matrix|live_execution_forbidden" tests/integration/test_paper_runtime.py` returns matches for all four strings.
  </acceptance_criteria>
  <action>Create `tests/integration/test_paper_runtime.py` in the repo's real-storage integration style. Add a once-mode test that uses a real SQLite database, a stubbed market-discovery payload, a stubbed NOAA path, and a stubbed terminal-state resolver to verify the runtime enters an allowed paper trade, persists the position and `paper_entry` event, and never calls any live-order execution function. Add a restoration test that seeds an open paper position directly in storage, runs the runtime again, and asserts restoration occurs before the resolution pass. Add a resolver-matrix test that feeds a market payload with non-terminal Polymarket fields and asserts no settlement occurs until the resolver marks the market terminal. Use exact string constants `paper-once`, `paper-loop`, `resolver_matrix`, and `live_execution_forbidden` in the test suite so the runtime contract remains explicit.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_paper_runtime.py -q</automated>
  </verify>
  <done>The runtime has pinned integration coverage for the once-mode paper flow, restart-safe open-position restoration, and fail-closed settlement behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reusable paper runtime orchestration with explicit settlement resolver rules</name>
  <read_first>
    - paper_trader.py
    - main.py
    - core/clob_client.py
    - core/storage.py
    - core/paper_execution.py
    - strategies/weather/signal_engine.py
    - tests/integration/test_paper_runtime.py
    - .planning/phases/04-paper-trading-runtime/04-CONTEXT.md
  </read_first>
  <files>core/paper_runtime.py, core/clob_client.py, core/paper_execution.py</files>
  <acceptance_criteria>
    - `rg -n "class PaperRuntime" core/paper_runtime.py` returns one match.
    - `rg -n "def run_once|def run_loop" core/paper_runtime.py` returns one match for each string.
    - `rg -n "RESOLVER_MATRIX|resolve_market_terminal_state" core/paper_runtime.py core/clob_client.py` returns matches for both strings.
    - `rg -n "closed|closedTime|resolvedBy|umaResolutionStatus" core/paper_runtime.py core/clob_client.py` returns matches for all four strings.
    - `rg -n "live_execution_forbidden" core/paper_runtime.py` returns one match.
    - `uv run --extra dev pytest tests/integration/test_paper_runtime.py -q` exits 0.
  </acceptance_criteria>
  <action>Create `core/paper_runtime.py` with a reusable `PaperRuntime` service that executes these ordered passes: restore open positions from storage, run candidate scan, run signal evaluation, run Phase 3 risk gating, create paper entries for `ALLOWED` decisions that do not already have an open position for the same `market_id` and `window_key`, poll open positions for settlement eligibility, and settle only positions whose market payloads satisfy an explicit resolver matrix. The resolver matrix must inspect these exact Polymarket-oriented fields when present in a market payload: `closed`, `closedTime`, `resolvedBy`, and `umaResolutionStatus`. Implement `resolve_market_terminal_state(...)` so unresolved, ambiguous, or missing terminal-state signals fail closed and keep the position open. Add an explicit guard constant or helper named `live_execution_forbidden` to make it impossible for this runtime to call any live-order path. If `core/clob_client.py` needs a narrow helper to normalize terminal-state fields for resolution polling, add it there without changing the Phase 1 discovery contract.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_paper_runtime.py -q</automated>
  </verify>
  <done>The repo has a reusable paper-runtime backend with a concrete, fail-closed settlement policy instead of an ad hoc or ambiguous runtime loop.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the placeholder paper_trader CLI with real paper-once and paper-loop execution paths</name>
  <read_first>
    - paper_trader.py
    - main.py
    - core/paper_runtime.py
    - config/settings.py
    - tests/integration/test_paper_runtime.py
    - .planning/phases/04-paper-trading-runtime/04-PATTERNS.md
  </read_first>
  <files>paper_trader.py, main.py</files>
  <acceptance_criteria>
    - `rg -n "paper-once|paper-loop" paper_trader.py` returns matches for both strings.
    - `rg -n "build_parser\\(" paper_trader.py` returns one match.
    - `rg -n "PaperRuntime" paper_trader.py` returns one match.
    - `rg -n "--database-path|--fixture|--interval" paper_trader.py` returns matches for all three strings.
    - `uv run --extra dev pytest tests/integration/test_paper_runtime.py -q` exits 0.
  </acceptance_criteria>
  <action>Replace the placeholder wrapper in `paper_trader.py` with a real CLI that mirrors the structure of `main.py` but exposes paper-runtime commands `paper-once` and `paper-loop`. Reuse the existing argument shape where it still fits: `--fixture`, `--database-path`, and `--interval`. Delegate all business logic to `PaperRuntime`; the script must not contain lifecycle math, settlement rules, or storage mutations directly. Keep `main.py` focused on discovery-only behavior, but extract any shared parser helpers that let both CLIs stay consistent without coupling discovery and paper runtime into one command tree. The paper CLI should print JSON summaries for each run so later observability work has a stable console contract.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_paper_runtime.py -q</automated>
  </verify>
  <done>The operator can run a dedicated paper-trading CLI in once or loop mode, and the implementation stays thin and paper-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovery/signal/risk pipeline -> paper runtime | Approved upstream decisions enter the runtime for paper execution |
| Polymarket market payload -> settlement resolver | External terminal-state fields decide whether open paper positions can be settled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T | `core/paper_runtime.py` | mitigate | Deduplicate entries by existing open position and require allowed risk decisions only |
| T-04-05 | S | `core/paper_runtime.py` | mitigate | Add an explicit `live_execution_forbidden` guard so no live-order path can be reached from paper mode |
| T-04-06 | T | `core/paper_runtime.py` | mitigate | Use a fail-closed resolver matrix for `closed`, `closedTime`, `resolvedBy`, and `umaResolutionStatus` so ambiguous market states do not settle trades prematurely |
</threat_model>

<verification>
Run `uv run --extra dev pytest tests/integration/test_paper_runtime.py -q`.
Run `uv run --extra dev pytest tests/integration/test_paper_runtime.py tests/integration/test_signal_risk_gate.py -q` after the plan completes to confirm the runtime composes with existing Phase 3 signal-risk behavior.
</verification>

<success_criteria>
The repo exposes a real paper-trading CLI and runtime service that can scan, evaluate, risk-gate, enter, restore, and resolve paper trades without ever touching a live-order path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-paper-trading-runtime/04-paper-trading-runtime-02-SUMMARY.md`
</output>
