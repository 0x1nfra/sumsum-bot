---
phase: 04-paper-trading-runtime
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - core/performance.py
  - paper_trader.py
  - tests/unit/test_performance_metrics.py
autonomous: true
requirements:
  - PAPR-03
must_haves:
  truths:
    - "Operator can measure bankroll delta, cumulative return, max drawdown, and drawdown-recovery behavior from durable Phase 4 records."
    - "Forward-test metrics are derived from append-only bankroll snapshots and resolved paper positions instead of transient loop counters."
    - "The runtime exposes a repeatable summary command without pulling dashboard work into Phase 4."
  artifacts:
    - path: "core/performance.py"
      provides: "Deterministic forward-test metrics over snapshots and resolved positions"
    - path: "paper_trader.py"
      provides: "CLI summary command for paper-trading performance"
    - path: "tests/unit/test_performance_metrics.py"
      provides: "Numeric regression coverage for return, drawdown, and recovery calculations"
  key_links:
    - from: "core/performance.py"
      to: "core/storage.py"
      via: "bankroll snapshots and resolved paper positions"
      pattern: "list_bankroll_snapshots|list_open_paper_positions"
    - from: "paper_trader.py"
      to: "core/performance.py"
      via: "paper-summary output"
      pattern: "calculate_forward_test_metrics|paper-summary"
---

<objective>
Add the forward-test performance layer so the operator can evaluate bankroll change and drawdown recovery over the 2-week paper-trading window.

Purpose: Close the measurement requirement of Phase 4 without dragging in Phase 5 dashboard scope.
Output: Deterministic metrics helpers plus a summary CLI over the durable paper ledger.
</objective>

<execution_context>
@/Users/irfanmurad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-paper-trading-runtime/04-CONTEXT.md
@.planning/phases/04-paper-trading-runtime/04-RESEARCH.md
@.planning/phases/04-paper-trading-runtime/04-VALIDATION.md
@.planning/phases/04-paper-trading-runtime/04-01-PLAN.md
@.planning/phases/04-paper-trading-runtime/04-02-PLAN.md
@core/storage.py
@paper_trader.py
@tests/unit/test_kelly_engine.py

<interfaces>
Pattern from `core/kelly_engine.py`:
```python
@dataclass(frozen=True)
class KellySizingDecision: ...

class KellyEngine:
    def calculate_position_size(...) -> KellySizingDecision: ...
```

Expected from `04-01`:
```python
@dataclass(frozen=True)
class BankrollSnapshotRecord: ...

@dataclass(frozen=True)
class PaperPositionRecord: ...
```
</interfaces>
</context>

## Source Audit

- `GOAL`: Covers the performance-measurement half of the Phase 4 goal.
- `REQ`: Satisfies `PAPR-03`.
- `RESEARCH`: Implements ledger-based metrics and keeps Phase 5 dashboard work deferred.
- `CONTEXT`: Implements `D-08` and `D-09` while preserving the deferred dashboard boundary.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add numeric regression tests for forward-test performance metrics</name>
  <files>tests/unit/test_performance_metrics.py</files>
  <read_first>tests/unit/test_kelly_engine.py, .planning/phases/04-paper-trading-runtime/04-VALIDATION.md, .planning/phases/04-paper-trading-runtime/04-RESEARCH.md, .planning/phases/04-paper-trading-runtime/04-01-PLAN.md</read_first>
  <acceptance_criteria>
    - `rg -n "test_forward_test_metrics_calculate_cumulative_return_and_bankroll_delta|test_forward_test_metrics_report_max_drawdown_and_recovery" tests/unit/test_performance_metrics.py` returns both metric tests.
    - The tests assert exact numeric values for bankroll delta, cumulative return percentage, max drawdown percentage, and recovery duration or recovery-not-yet-achieved state.
  </acceptance_criteria>
  <behavior>
    - Test 1: Metrics compute `bankroll_change_usd` and `cumulative_return_pct` from the first and last durable bankroll snapshots.
    - Test 2: Metrics compute `max_drawdown_pct` from peak-to-trough total bankroll and mark recovery complete only when a later snapshot reaches or exceeds the prior peak.
    - Test 3: Unresolved/open positions do not count as realized wins or losses in the summary.
  </behavior>
  <action>Create `tests/unit/test_performance_metrics.py` with small snapshot and resolved-position fixtures that pin exact values for `starting_bankroll_usd`, `ending_bankroll_usd`, `bankroll_change_usd`, `cumulative_return_pct`, `max_drawdown_pct`, `recovered_from_max_drawdown`, and `recovery_snapshot_index`. Use deterministic numeric assertions, not loose ranges.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_performance_metrics.py -q</automated>
  </verify>
  <done>The forward-test measurement rules are executable and exact before the implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ledger-based forward-test metrics and a paper-summary command</name>
  <files>core/performance.py, paper_trader.py</files>
  <read_first>core/storage.py, paper_trader.py, tests/unit/test_performance_metrics.py, .planning/phases/04-paper-trading-runtime/04-CONTEXT.md, .planning/phases/04-paper-trading-runtime/04-PATTERNS.md</read_first>
  <acceptance_criteria>
    - `rg -n "class ForwardTestMetrics|def calculate_forward_test_metrics" core/performance.py` returns the metrics API.
    - `rg -n "paper-summary|starting_bankroll_usd|ending_bankroll_usd|bankroll_change_usd|cumulative_return_pct|max_drawdown_pct|recovered_from_max_drawdown" paper_trader.py` returns the summary command and output fields.
    - `rg -n "dashboard|http|fastapi|flask" core/performance.py paper_trader.py` returns no matches.
  </acceptance_criteria>
  <behavior>
    - Test 1: `calculate_forward_test_metrics()` reads durable snapshots and resolved positions only; it does not depend on in-memory runtime counters.
    - Test 2: `paper-summary` prints one JSON object with exact metric keys for later Phase 5 consumption.
    - Test 3: Missing or insufficient snapshot history returns an explicit empty-summary shape instead of crashing.
  </behavior>
  <action>Create `core/performance.py` with an immutable `ForwardTestMetrics` dataclass and `calculate_forward_test_metrics(snapshots, resolved_positions)` helper. Compute metrics from durable records only: `starting_bankroll_usd`, `ending_bankroll_usd`, `bankroll_change_usd`, `cumulative_return_pct`, `max_drawdown_pct`, `resolved_trade_count`, `winning_trade_count`, `win_rate_pct`, `recovered_from_max_drawdown`, and `recovery_snapshot_index`. Update `paper_trader.py` to add a `paper-summary` subcommand that loads the SQLite store, reads bankroll snapshots and resolved paper positions, calls `calculate_forward_test_metrics()`, and prints a single JSON summary object. Keep the command paper-only and backend-only; do not add dashboard routes, templates, or web dependencies.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_performance_metrics.py -q</automated>
  </verify>
  <done>The operator can compute forward-test performance from the paper ledger with a stable CLI output that Phase 5 can later consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite ledger -> performance calculations | Metrics must trust only durable records and deterministic ordering. |
| CLI summary output -> operator decisions | The summary becomes the operator’s source of truth for forward-test progress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | T | `core/performance.py` metrics rollup | mitigate | Derive metrics from append-only bankroll snapshots and terminal resolved positions only, with deterministic first/last and peak/trough ordering. |
| T-04-03-02 | I | `paper_trader.py` summary command | accept | Output contains only trading metrics and no credentials or PII; keep the command JSON-only and paper-only. |
| T-04-03-03 | D | `core/performance.py` sparse-history path | mitigate | Return an explicit empty-summary shape when snapshot history is missing instead of crashing the operator summary command. |
</threat_model>

<verification>
Run `uv run --extra dev pytest tests/unit/test_performance_metrics.py -q`.
</verification>

<success_criteria>
Forward-test return, drawdown, and recovery metrics are computed from durable paper-trading records and exposed through a stable paper-summary CLI for the operator.
</success_criteria>

<output>
After completion, create `.planning/phases/04-paper-trading-runtime/04-03-SUMMARY.md`
</output>
