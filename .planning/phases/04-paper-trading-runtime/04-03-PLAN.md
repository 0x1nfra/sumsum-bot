---
phase: 04-paper-trading-runtime
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - core/models.py
  - core/storage.py
  - core/performance.py
  - paper_trader.py
  - tests/unit/test_performance_metrics.py
  - tests/integration/test_paper_runtime.py
autonomous: true
requirements: [PAPR-03]
must_haves:
  truths:
    - "Forward-test metrics are derived from durable bankroll and paper-trade history rather than transient in-memory counters."
    - "The operator can measure cumulative return, bankroll change, drawdown, and drawdown recovery over the paper-trading window."
    - "Phase 4 reporting remains CLI- and storage-level only; it does not drift into Phase 5 dashboard scope."
  artifacts:
    - path: "core/performance.py"
      provides: "Deterministic forward-test metric calculations"
    - path: "tests/unit/test_performance_metrics.py"
      provides: "Pinned numeric coverage for return, drawdown, and recovery calculations"
    - path: "paper_trader.py"
      provides: "JSON runtime summaries exposing forward-test metrics"
  key_links:
    - from: "core/storage.py"
      to: "core/performance.py"
      via: "bankroll snapshots and resolved positions feed metric calculations"
      pattern: "list_bankroll_snapshots|list_resolved_paper_positions|calculate_forward_test_metrics"
    - from: "core/performance.py"
      to: "paper_trader.py"
      via: "CLI summary exposes computed metrics"
      pattern: "ForwardTestMetrics"
---

<objective>
Add the forward-test analytics layer for Phase 4: deterministic calculations over stored bankroll history and resolved paper trades, plus JSON summaries from the paper runtime that report cumulative return, bankroll delta, drawdown, and drawdown-recovery behavior.

Purpose: satisfy PAPR-03 and finish Phase 4 without building the Phase 5 dashboard.
Output: performance models and calculations, storage query helpers for metrics inputs, and passing unit/integration coverage for forward-test reporting.
</objective>

<execution_context>
@/Users/irfanmurad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-paper-trading-runtime/04-CONTEXT.md
@.planning/phases/04-paper-trading-runtime/04-RESEARCH.md
@.planning/phases/04-paper-trading-runtime/04-VALIDATION.md
@.planning/phases/04-paper-trading-runtime/04-PATTERNS.md
@paper_trader.py
@core/storage.py
@core/models.py
@core/paper_runtime.py
@tests/integration/test_paper_runtime.py

<interfaces>
From core/storage.py after 04-01:
```python
class CandidateStorage:
    def persist_bankroll_snapshots(...) -> int:
        ...
```

From paper_trader.py after 04-02:
```python
def main(argv: Sequence[str] | None = None) -> int:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red-first unit tests for forward-test metric calculations</name>
  <read_first>
    - core/models.py
    - core/storage.py
    - tests/unit/test_kelly_engine.py
    - .planning/phases/04-paper-trading-runtime/04-RESEARCH.md
    - .planning/phases/04-paper-trading-runtime/04-VALIDATION.md
  </read_first>
  <files>tests/unit/test_performance_metrics.py</files>
  <acceptance_criteria>
    - `rg -n "test_forward_test_metrics_compute_cumulative_return_and_bankroll_delta" tests/unit/test_performance_metrics.py` returns one match.
    - `rg -n "test_forward_test_metrics_compute_max_drawdown_and_recovery_duration" tests/unit/test_performance_metrics.py` returns one match.
    - `rg -n "cumulative_return_pct|max_drawdown_pct|drawdown_recovery_steps" tests/unit/test_performance_metrics.py` returns matches for all three strings.
  </acceptance_criteria>
  <action>Create `tests/unit/test_performance_metrics.py` with explicit numeric assertions over a small in-memory ledger fixture. The first test must build bankroll snapshots whose starting bankroll is `100.0` and ending bankroll is `108.0`, then assert exact `cumulative_return_pct=8.0` and `bankroll_delta_usd=8.0`. The second test must build a sequence that peaks at `110.0`, drops to `88.0`, and later recovers to `111.0`, then assert exact `max_drawdown_pct=20.0` and a nonzero `drawdown_recovery_steps` field. Keep the tests pure and deterministic; do not rely on the CLI or live runtime loop.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_performance_metrics.py -q</automated>
  </verify>
  <done>The forward-test analytics contract is pinned with deterministic, numeric unit coverage before implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Implement storage query helpers and deterministic forward-test metrics</name>
  <read_first>
    - core/storage.py
    - core/models.py
    - tests/unit/test_performance_metrics.py
    - tests/integration/test_paper_runtime.py
    - .planning/phases/04-paper-trading-runtime/04-PATTERNS.md
  </read_first>
  <files>core/models.py, core/storage.py, core/performance.py</files>
  <acceptance_criteria>
    - `rg -n "class ForwardTestMetrics" core/models.py core/performance.py` returns one match.
    - `rg -n "def calculate_forward_test_metrics" core/performance.py` returns one match.
    - `rg -n "list_bankroll_snapshots|list_resolved_paper_positions" core/storage.py` returns matches for both strings.
    - `rg -n "cumulative_return_pct|max_drawdown_pct|drawdown_recovery_steps" core/performance.py` returns matches for all three strings.
    - `uv run --extra dev pytest tests/unit/test_performance_metrics.py -q` exits 0.
  </acceptance_criteria>
  <action>Create `core/performance.py` with a deterministic public entrypoint `calculate_forward_test_metrics(bankroll_snapshots: list[BankrollSnapshot], resolved_positions: list[PaperPositionRecord]) -> ForwardTestMetrics`. Add or extend a `ForwardTestMetrics` dataclass so it includes `starting_bankroll_usd`, `current_bankroll_usd`, `bankroll_delta_usd`, `cumulative_return_pct`, `max_drawdown_pct`, `drawdown_recovery_steps`, `resolved_trade_count`, and `win_rate_pct`. Extend `CandidateStorage` with query helpers `list_bankroll_snapshots()` and `list_resolved_paper_positions()` that return ordered records suitable for metrics calculation. Implement the metrics logic so `cumulative_return_pct` is measured from the first to the latest bankroll snapshot, `max_drawdown_pct` uses the rolling peak-to-trough percentage, and `drawdown_recovery_steps` records how many snapshots elapse before a drawdown fully recovers. Keep the implementation backend-only and free of dashboard concerns.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_performance_metrics.py -q</automated>
  </verify>
  <done>The repo has a deterministic metrics layer over the durable Phase 4 ledger instead of ad hoc runtime counters.</done>
</task>

<task type="auto">
  <name>Task 3: Expose forward-test metrics in paper runtime summaries without expanding into dashboard scope</name>
  <read_first>
    - paper_trader.py
    - core/paper_runtime.py
    - core/performance.py
    - tests/integration/test_paper_runtime.py
    - tests/unit/test_performance_metrics.py
    - .planning/phases/04-paper-trading-runtime/04-CONTEXT.md
  </read_first>
  <files>paper_trader.py, tests/integration/test_paper_runtime.py</files>
  <acceptance_criteria>
    - `rg -n "cumulative_return_pct|max_drawdown_pct|drawdown_recovery_steps|resolved_trade_count" paper_trader.py` returns matches for all four strings.
    - `rg -n "test_paper_runtime_summary_reports_forward_test_metrics" tests/integration/test_paper_runtime.py` returns one match.
    - `uv run --extra dev pytest tests/unit/test_performance_metrics.py tests/integration/test_paper_runtime.py -q` exits 0.
  </acceptance_criteria>
  <action>Update `paper_trader.py` and, if needed, the runtime summary objects so `paper-once` and `paper-loop` JSON outputs include the computed Phase 4 forward-test metrics `cumulative_return_pct`, `bankroll_delta_usd`, `max_drawdown_pct`, `drawdown_recovery_steps`, and `resolved_trade_count`. Add `test_paper_runtime_summary_reports_forward_test_metrics` to `tests/integration/test_paper_runtime.py` and assert those exact keys exist in the emitted summary after seeding resolved trades and bankroll snapshots. Keep the output console/JSON-only; do not add HTTP endpoints, HTML, or dashboard rendering in this phase.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_performance_metrics.py tests/integration/test_paper_runtime.py -q</automated>
  </verify>
  <done>The operator can inspect the key forward-test metrics from the paper runtime directly, and the implementation remains within Phase 4 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite paper ledger -> analytics layer | Persisted trade and bankroll records become operator-facing forward-test metrics |
| analytics layer -> CLI summaries | Calculated metrics are exposed in runtime output used for monitoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T | `core/performance.py` | mitigate | Derive metrics from ordered bankroll snapshots and resolved positions only, not mutable counters |
| T-04-08 | I | `paper_trader.py` | mitigate | Emit exact metric keys from the deterministic metrics object so summaries cannot silently omit required forward-test fields |
| T-04-09 | D | `core/storage.py` | mitigate | Query helpers must return ordered durable records so metric calculations survive restarts and long-running forward tests |
</threat_model>

<verification>
Run `uv run --extra dev pytest tests/unit/test_performance_metrics.py -q`.
Run `uv run --extra dev pytest tests/unit/test_performance_metrics.py tests/integration/test_paper_runtime.py -q` after the plan completes to confirm runtime summaries expose the durable metrics correctly.
</verification>

<success_criteria>
Phase 4 can report bankroll change, cumulative return, drawdown, and drawdown recovery from durable paper-trading history, and those metrics are available from the paper runtime without needing the Phase 5 dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/04-paper-trading-runtime/04-paper-trading-runtime-03-SUMMARY.md`
</output>
