---
phase: 01-market-discovery-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - "00"
  - "02"
  - "03"
files_modified:
  - core/clob_client.py
  - core/market_scanner.py
  - strategies/weather/types.py
  - strategies/weather/normalization.py
  - strategies/weather/scanner.py
  - tests/unit/test_weather_normalization.py
  - tests/integration/test_polymarket_ingestion.py
autonomous: true
requirements:
  - DISC-01
user_setup: []
must_haves:
  truths:
    - The system can ingest Polymarket weather market data and normalize it into explicit candidate models.
    - Supported and unsupported weather markets are separated intentionally instead of guessed through ambiguous parsing.
    - The shared scanner pipeline can return normalized weather-market candidates for downstream filtering.
  artifacts:
    - core/clob_client.py
    - core/market_scanner.py
    - strategies/weather/normalization.py
    - strategies/weather/scanner.py
    - tests/unit/test_weather_normalization.py
    - tests/integration/test_polymarket_ingestion.py
  key_links:
    - core/market_scanner.py uses core/clob_client.py for remote market fetches.
    - strategies/weather/scanner.py consumes normalized weather-market objects instead of raw payload dictionaries.
    - tests cover approved, review, and rejected normalization outcomes.
---

<objective>
Implement Polymarket weather market ingestion and normalization with explicit support tiers.

Purpose: Satisfy the first half of market discovery by making weather markets inspectable as stable domain objects before any liquidity or price gating is applied.
Output: Polymarket client adapter, weather normalization logic, scanner orchestration hooks, and tests covering approved and rejected contract shapes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-market-discovery-foundation/01-CONTEXT.md
@.planning/phases/01-market-discovery-foundation/01-RESEARCH.md
@.planning/phases/01-market-discovery-foundation/01-VALIDATION.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in This Plan |
|--------|----------|-------------------------|
| T-1-03 Malformed or incomplete Polymarket payloads produce invalid candidates | medium | Validate payload shape in `core/clob_client.py` and reject incomplete records before normalization. |
| T-1-04 Ambiguous weather titles are treated as tradable | high | Implement tiered normalization with explicit approved, review, and rejected outcomes in `strategies/weather/normalization.py`. |
| T-1-05 Scanner logic becomes transport-specific | low | Keep transport handling in `core/clob_client.py` and normalization in weather strategy modules. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the Polymarket market-data adapter and scanner handoff</name>
  <files>core/clob_client.py, core/market_scanner.py, tests/integration/test_polymarket_ingestion.py</files>
  <read_first>core/storage.py, config/settings.py, docs/prd.md, .planning/phases/01-market-discovery-foundation/01-RESEARCH.md</read_first>
  <action>Implement `core/clob_client.py` as the adapter responsible for fetching or accepting Polymarket market payloads and returning a normalized raw-market transport structure for the rest of the app. Shape the adapter around a public discovery payload that can be exercised through fixtures in tests, while keeping the transport boundary narrow enough to swap endpoints later if needed. Update `core/market_scanner.py` so it loads settings, calls the client, and passes only weather-relevant records into the weather scanner. Add `tests/integration/test_polymarket_ingestion.py` with fixture payloads that prove the adapter rejects malformed payloads and hands off valid weather-market records.</action>
  <verify>python -m pytest tests/integration/test_polymarket_ingestion.py -q</verify>
  <acceptance_criteria>
    - `core/clob_client.py` exists and contains a client class or fetch function
    - `core/market_scanner.py` imports `core.clob_client`
    - `tests/integration/test_polymarket_ingestion.py` exits 0
    - ingestion tests include at least one malformed payload case
  </acceptance_criteria>
  <done>Market ingestion is isolated behind a dedicated adapter and exposed through a shared scanner entry.</done>
</task>

<task type="auto">
  <name>Task 2: Build tiered weather-market normalization</name>
  <files>strategies/weather/types.py, strategies/weather/normalization.py, tests/unit/test_weather_normalization.py</files>
  <read_first>core/models.py, .planning/phases/01-market-discovery-foundation/01-CONTEXT.md, .planning/phases/01-market-discovery-foundation/01-RESEARCH.md</read_first>
  <action>Create weather-specific types in `strategies/weather/types.py` and implement `strategies/weather/normalization.py` to parse contract titles into explicit fields such as market type, location text, threshold, resolution time, and support status. The normalization path must return approved candidates only when all required fields are trustworthy; otherwise it must return review or rejected outcomes with exact reason codes or reason strings. Restrict full support in Phase 1 to the initially approved weather contract families documented in research, and route all other formats to review or rejection. Add `tests/unit/test_weather_normalization.py` covering at least one approved market, one ambiguous market routed to review, and one unsupported market routed to rejection.</action>
  <verify>python -m pytest tests/unit/test_weather_normalization.py -q</verify>
  <acceptance_criteria>
    - `strategies/weather/normalization.py` contains support-tier logic
    - normalization code emits approved and non-approved outcomes
    - `tests/unit/test_weather_normalization.py` exits 0
    - normalization tests assert exact rejection or review reasons
  </acceptance_criteria>
  <done>Weather contracts are normalized conservatively and ambiguous inputs cannot become tradable candidates silently.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the weather scanner to return normalized candidate sets</name>
  <files>strategies/weather/scanner.py, core/market_scanner.py</files>
  <read_first>core/market_scanner.py, strategies/weather/normalization.py, core/models.py</read_first>
  <action>Implement `strategies/weather/scanner.py` so it consumes raw weather-market transport objects, applies normalization, and returns structured collections for approved candidates and non-approved records. Update `core/market_scanner.py` to call the weather scanner and return a single scan result object that Plan 05 can enrich with filtering, persistence, and continuous-scan reporting. Keep this stage limited to ingestion and normalization only; do not apply liquidity or price filters here yet.</action>
  <verify>python -m pytest tests/unit/test_weather_normalization.py tests/integration/test_polymarket_ingestion.py -q</verify>
  <acceptance_criteria>
    - `strategies/weather/scanner.py` imports the normalization module
    - `core/market_scanner.py` returns approved and non-approved groupings
    - no filter-threshold logic is embedded in `strategies/weather/normalization.py`
  </acceptance_criteria>
  <done>The shared scan flow can hand downstream phases a normalized set of weather-market records.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/unit/test_weather_normalization.py -q`
- [ ] `python -m pytest tests/integration/test_polymarket_ingestion.py -q`
- [ ] `test -f core/clob_client.py && test -f strategies/weather/normalization.py && test -f strategies/weather/scanner.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Weather-market ingestion and normalization exist behind stable shared modules
- Unsupported or ambiguous markets are explicitly marked non-tradable
</success_criteria>

<output>
After completion, create `.planning/phases/01-market-discovery-foundation/01-04-SUMMARY.md`
</output>
