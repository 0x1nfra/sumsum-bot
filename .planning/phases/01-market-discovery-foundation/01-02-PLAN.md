---
phase: 01-market-discovery-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "00"
  - "01"
files_modified:
  - config/settings.py
  - config/kill_switches.py
  - core/kelly_engine.py
  - core/models.py
  - core/risk_manager.py
  - core/storage.py
  - core/trade_logger.py
  - core/backtester.py
autonomous: true
requirements:
  - DISC-01
user_setup: []
must_haves:
  truths:
    - The project has typed scanner settings with code defaults and an environment override path.
    - Candidate persistence is isolated behind a reusable SQLite-first storage layer.
    - Shared candidate-domain models exist before market ingestion logic lands.
  artifacts:
    - config/settings.py
    - core/kelly_engine.py
    - core/models.py
    - core/risk_manager.py
    - core/storage.py
    - core/trade_logger.py
    - core/backtester.py
  key_links:
    - core/storage.py reads database configuration from config/settings.py.
    - core/models.py defines candidate primitives used by storage and later scanner logic.
    - this plan uniquely owns creation of the shared core placeholder modules it introduces.
    - tests verify settings parsing and storage bootstrap through the new shared foundation.
---

<objective>
Implement the typed settings, shared candidate models, and SQLite-first storage foundation.

Purpose: Separate structural bootstrap from executable foundations so Phase 1 gets a real config and persistence layer without overloading the first plan.
Output: Typed settings, shared models, reusable storage APIs, and tests covering defaults, overrides, and candidate persistence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-market-discovery-foundation/01-CONTEXT.md
@.planning/phases/01-market-discovery-foundation/01-RESEARCH.md
@docs/prd.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in This Plan |
|--------|----------|-------------------------|
| T-1-02 Config drift or missing thresholds cause unsafe scanner behavior | medium | Centralize defaults and override parsing in `config/settings.py` with tests. |
| T-1-03 Direct SQLite usage leaks across modules and blocks later migration | medium | Keep database wiring and CRUD operations behind `core/storage.py` and shared models. |
| T-1-04 Candidate records lack stable schema before scanner implementation | low | Define shared domain models before ingestion and filtering layers write data. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add typed settings for scanner and storage configuration</name>
  <files>config/settings.py, config/kill_switches.py</files>
  <read_first>.planning/phases/01-market-discovery-foundation/01-CONTEXT.md, .planning/phases/01-market-discovery-foundation/01-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <action>Implement `config/settings.py` with a typed settings class or equivalent model that defines the scanner database path, minimum liquidity, maximum entry price, maximum hours to resolution, and any scan command defaults required by Phase 1. The settings layer must provide code defaults and an environment override path suitable for later deployment. Add a thin `config/kill_switches.py` placeholder so later risk work extends an existing config boundary. Make the implementation satisfy the Wave 0 `tests/unit/test_settings.py` contract for default values and override parsing.</action>
  <verify>python -m pytest tests/unit/test_settings.py -q</verify>
  <acceptance_criteria>
    - `config/settings.py` defines a settings class or equivalent typed model
    - `config/settings.py` contains fields for liquidity, price ceiling, and resolution-window thresholds
    - `tests/unit/test_settings.py` exits 0
    - settings tests cover environment overrides
  </acceptance_criteria>
  <done>Configuration is explicit, typed, and safe to extend in later deployment phases.</done>
</task>

<task type="auto">
  <name>Task 2: Define shared candidate-domain models and future core placeholders</name>
  <files>core/models.py, core/kelly_engine.py, core/risk_manager.py, core/trade_logger.py, core/backtester.py</files>
  <read_first>config/settings.py, .planning/phases/01-market-discovery-foundation/01-CONTEXT.md, .planning/phases/01-market-discovery-foundation/01-RESEARCH.md</read_first>
  <action>Create `core/models.py` with the shared domain primitives needed across the phase: scan result metadata, accepted candidate records, rejected candidate records, candidate status, and rejection reason fields. In the same plan, create thin real placeholders for `core/kelly_engine.py`, `core/risk_manager.py`, `core/trade_logger.py`, and `core/backtester.py` so the documented core tree exists on disk before later phases implement those behaviors.</action>
  <verify>test -f core/models.py && test -f core/kelly_engine.py && test -f core/risk_manager.py && test -f core/trade_logger.py && test -f core/backtester.py && rg -n \"candidate|rejection|scan\" core/models.py</verify>
  <acceptance_criteria>
    - `core/models.py` exists
    - `core/models.py` contains candidate status and rejection reason structures
    - `core/models.py` contains a scan result model or equivalent primitive
    - `core/kelly_engine.py` exists
    - `core/risk_manager.py` exists
  </acceptance_criteria>
  <done>Later plans can share one stable candidate vocabulary while future core module boundaries already exist on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the SQLite-first storage abstraction</name>
  <files>core/storage.py</files>
  <read_first>config/settings.py, core/models.py, .planning/phases/01-market-discovery-foundation/01-RESEARCH.md</read_first>
  <action>Implement `core/storage.py` as the only module responsible for database bootstrap and candidate persistence. It must initialize the SQLite database path from settings, create schema objects or tables for scan runs, accepted candidates, review-state candidates, and rejected candidates, and expose repository-style methods to save and query scan outputs. Make the implementation satisfy the Wave 0 `tests/integration/test_storage.py` contract for explicit reason persistence and status separation.</action>
  <verify>python -m pytest tests/integration/test_storage.py -q</verify>
  <acceptance_criteria>
    - `core/storage.py` contains a storage class or repository functions
    - `core/storage.py` contains schema or table definitions for accepted and rejected candidates
    - `tests/integration/test_storage.py` exits 0
    - storage tests assert a rejection reason is persisted
  </acceptance_criteria>
  <done>Phase 1 has a reusable persistence layer ready for scanner outputs and later paper-trading history.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/unit/test_settings.py -q`
- [ ] `python -m pytest tests/integration/test_storage.py -q`
- [ ] `test -f config/settings.py && test -f core/models.py && test -f core/storage.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Typed settings and SQLite-backed storage abstractions are implemented behind stable shared modules
- Later plans can rely on config and persistence foundations without rewriting them
</success_criteria>

<output>
After completion, create `.planning/phases/01-market-discovery-foundation/01-02-SUMMARY.md`
</output>
