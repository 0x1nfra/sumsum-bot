---
phase: 01-market-discovery-foundation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/fixtures/polymarket_weather_markets.json
  - tests/unit/test_project_layout.py
  - tests/unit/test_settings.py
  - tests/unit/test_weather_normalization.py
  - tests/unit/test_filter_rules.py
  - tests/integration/test_storage.py
  - tests/integration/test_polymarket_ingestion.py
  - tests/integration/test_market_scan.py
autonomous: true
requirements:
  - DISC-01
  - DISC-02
user_setup: []
must_haves:
  truths:
    - Phase 1 has a collectable pytest harness with shared fixtures before feature implementation begins.
    - The validation layer can exercise layout, settings, normalization, filtering, ingestion, and scan behaviors once implementation lands.
    - Later plans can verify behavior with executable commands backed by shared fixtures instead of placeholder checks.
  artifacts:
    - pyproject.toml
    - tests/conftest.py
    - tests/fixtures/polymarket_weather_markets.json
    - tests/unit/test_project_layout.py
    - tests/unit/test_settings.py
    - tests/unit/test_weather_normalization.py
    - tests/unit/test_filter_rules.py
    - tests/integration/test_storage.py
    - tests/integration/test_polymarket_ingestion.py
    - tests/integration/test_market_scan.py
  key_links:
    - The validation contract in `01-VALIDATION.md` maps directly to files created in this plan.
    - Shared fixture data is reused by ingestion, scan, and operator-command verification.
---

<objective>
Create the Wave 0 validation harness referenced by the phase validation strategy.

Purpose: Satisfy the Nyquist validation gate before feature work by creating the test and fixture surface that later plans verify against.
Output: Python project test harness, shared fixtures, and all Phase 1 unit and integration test files.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-market-discovery-foundation/01-VALIDATION.md
@.planning/phases/01-market-discovery-foundation/01-RESEARCH.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in This Plan |
|--------|----------|-------------------------|
| T-1-00 Later plans cannot prove behavior because no test harness exists | high | Add `pyproject.toml`, pytest wiring, and all referenced Phase 1 test files up front. |
| T-1-01 Verification relies on ad hoc fixtures or manual payload editing | medium | Create a shared weather-market fixture file in `tests/fixtures/`. |
| T-1-02 Validation and plan verification drift apart | medium | Match the exact test files and commands referenced in `01-VALIDATION.md`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the Python test command surface and shared fixture wiring</name>
  <files>pyproject.toml, tests/conftest.py, tests/fixtures/polymarket_weather_markets.json</files>
  <read_first>.planning/phases/01-market-discovery-foundation/01-VALIDATION.md, .planning/phases/01-market-discovery-foundation/01-RESEARCH.md</read_first>
  <action>Create `pyproject.toml` with a Python project definition and pytest command surface, add `tests/conftest.py` for shared test helpers, and add `tests/fixtures/polymarket_weather_markets.json` containing representative approved, review, and rejected weather-market payload examples for ingestion and scan tests.</action>
  <verify>test -f pyproject.toml && test -f tests/conftest.py && test -f tests/fixtures/polymarket_weather_markets.json</verify>
  <acceptance_criteria>
    - `pyproject.toml` exists and contains `[project]`
    - `pyproject.toml` contains `pytest`
    - `tests/conftest.py` exists
    - `tests/fixtures/polymarket_weather_markets.json` exists
  </acceptance_criteria>
  <done>All later plans have a shared Python test harness and fixture source.</done>
</task>

<task type="auto">
  <name>Task 2: Create the unit-test files required by the validation map</name>
  <files>tests/unit/test_project_layout.py, tests/unit/test_settings.py, tests/unit/test_weather_normalization.py, tests/unit/test_filter_rules.py</files>
  <read_first>.planning/phases/01-market-discovery-foundation/01-VALIDATION.md, docs/prd.md</read_first>
  <action>Create the Phase 1 unit-test files referenced in `01-VALIDATION.md` so later plans can satisfy or extend them: layout, settings, weather normalization, and filter-rule tests. Each file must contain real `test_` functions with executable assertions that describe expected behavior, not empty placeholders or comments.</action>
  <verify>rg -n \"def test_|assert \" tests/unit/test_project_layout.py tests/unit/test_settings.py tests/unit/test_weather_normalization.py tests/unit/test_filter_rules.py && python -m pytest --collect-only tests/unit/test_project_layout.py tests/unit/test_settings.py tests/unit/test_weather_normalization.py tests/unit/test_filter_rules.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_project_layout.py` exists
    - `tests/unit/test_settings.py` exists
    - `tests/unit/test_weather_normalization.py` exists
    - `tests/unit/test_filter_rules.py` exists
    - each unit test file contains executable assertions
  </acceptance_criteria>
  <done>The unit-test surface required by the validation strategy exists.</done>
</task>

<task type="auto">
  <name>Task 3: Create the integration-test files required by the validation map</name>
  <files>tests/integration/test_storage.py, tests/integration/test_polymarket_ingestion.py, tests/integration/test_market_scan.py</files>
  <read_first>.planning/phases/01-market-discovery-foundation/01-VALIDATION.md, tests/fixtures/polymarket_weather_markets.json</read_first>
  <action>Create the Phase 1 integration-test files referenced in `01-VALIDATION.md` so later plans can satisfy or extend them: storage, Polymarket ingestion, and end-to-end market scanning tests. Each file must contain real `test_` functions with executable assertions against the shared fixture file, not empty placeholders or comments.</action>
  <verify>rg -n \"def test_|assert \" tests/integration/test_storage.py tests/integration/test_polymarket_ingestion.py tests/integration/test_market_scan.py && python -m pytest --collect-only tests/integration/test_storage.py tests/integration/test_polymarket_ingestion.py tests/integration/test_market_scan.py -q</verify>
  <acceptance_criteria>
    - `tests/integration/test_storage.py` exists
    - `tests/integration/test_polymarket_ingestion.py` exists
    - `tests/integration/test_market_scan.py` exists
    - each integration test file contains executable assertions
  </acceptance_criteria>
  <done>The integration-test surface required by the validation strategy exists.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f pyproject.toml && test -f tests/conftest.py && test -f tests/fixtures/polymarket_weather_markets.json`
- [ ] `python -m pytest --collect-only tests/unit/test_project_layout.py tests/unit/test_settings.py tests/unit/test_weather_normalization.py tests/unit/test_filter_rules.py -q`
- [ ] `python -m pytest --collect-only tests/integration/test_storage.py tests/integration/test_polymarket_ingestion.py tests/integration/test_market_scan.py -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Wave 0 validation files referenced by `01-VALIDATION.md` exist
- Later plans can use executable verification commands backed by shared fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/01-market-discovery-foundation/01-00-SUMMARY.md`
</output>
