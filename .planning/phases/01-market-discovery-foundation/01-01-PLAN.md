---
phase: 01-market-discovery-foundation
plan: "01"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - main.py
  - paper_trader.py
  - config/__init__.py
  - core/__init__.py
  - strategies/__init__.py
  - strategies/weather/__init__.py
  - backtest/runner.py
  - backtest/historical/.gitkeep
  - data/.gitkeep
autonomous: true
requirements:
  - DISC-01
user_setup: []
must_haves:
  truths:
    - The repository has runnable root entrypoints and package anchors for the Phase 1 skeleton buildout.
    - Root, package, and backtest anchor paths exist before core and strategy modules are extended in sibling Wave 1 plans.
    - Later Wave 1 plans can fill in the documented module tree without restructuring the repo root.
  artifacts:
    - main.py
    - paper_trader.py
    - config/__init__.py
    - core/__init__.py
    - strategies/__init__.py
    - strategies/weather/__init__.py
    - backtest/runner.py
  key_links:
    - main.py and paper_trader.py import shared modules instead of embedding business logic.
    - This plan provides the repo-root anchors that sibling Wave 1 plans extend into the full documented skeleton.
---

<objective>
Create the repo-root anchors and thin entrypoints for the documented MVP skeleton.

Purpose: Establish the root-level structure up front while sibling Wave 1 plans complete the full documented module tree without changing ownership boundaries.
Output: Thin entrypoints, package anchors, and backtest paths.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-market-discovery-foundation/01-CONTEXT.md
@.planning/phases/01-market-discovery-foundation/01-RESEARCH.md
@docs/prd.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in This Plan |
|--------|----------|-------------------------|
| T-1-01 Future phases diverge from the documented architecture because planned module locations do not exist | medium | Create every documented module path now as a thin real file or package. |
| T-1-02 Entry scripts accumulate business logic and become hard to test | low | Keep `main.py` and `paper_trader.py` thin and import shared modules only. |
| T-1-03 Deferred strategies leak behavior into Phase 1 | low | Create only placeholder modules for BTC, sports, and backtest paths without implementing out-of-scope strategy logic. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the thin entrypoints and package anchors</name>
  <files>main.py, paper_trader.py, config/__init__.py, core/__init__.py, strategies/__init__.py, strategies/weather/__init__.py, data/.gitkeep</files>
  <read_first>docs/prd.md, .planning/phases/01-market-discovery-foundation/01-CONTEXT.md</read_first>
  <action>Add thin `main.py` and `paper_trader.py` entrypoints that import shared modules and return placeholder runtime status without embedding scanner logic. Add `__init__.py` files for `config`, `core`, `strategies`, and `strategies/weather`, plus `data/.gitkeep`, so the repo becomes a runnable Python project instead of docs-only scaffolding. Keep ownership in this plan limited to root anchors and entrypoints; do not create the later core or deferred strategy placeholder files here.</action>
  <verify>test -f main.py && test -f paper_trader.py && test -f config/__init__.py && test -f core/__init__.py && test -f strategies/weather/__init__.py && test -f data/.gitkeep</verify>
  <acceptance_criteria>
    - `main.py` imports from `config` or `core`
    - `paper_trader.py` exists and does not contain scanner business logic beyond a thin entrypoint
    - `data/.gitkeep` exists
  </acceptance_criteria>
  <done>The repository has a runnable Python root with thin entrypoints and package boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add the backtest path and root anchors</name>
  <files>backtest/runner.py, backtest/historical/.gitkeep</files>
  <read_first>docs/prd.md, .planning/phases/01-market-discovery-foundation/01-CONTEXT.md, .planning/phases/01-market-discovery-foundation/01-RESEARCH.md</read_first>
  <action>Create the `backtest/` path from the documented MVP tree by adding a thin `backtest/runner.py` placeholder and `backtest/historical/.gitkeep`. Keep both files explicitly minimal in Phase 1; they only need to establish the future boundary and avoid another structural rewrite later.</action>
  <verify>test -f backtest/runner.py && test -f backtest/historical/.gitkeep</verify>
  <acceptance_criteria>
    - `backtest/runner.py` exists
    - `backtest/historical/.gitkeep` exists
  </acceptance_criteria>
  <done>The backtest path exists up front as part of the documented MVP structure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f main.py && test -f paper_trader.py && test -f backtest/runner.py`
- [ ] `test -f config/__init__.py && test -f core/__init__.py && test -f strategies/weather/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository root is anchored for the full documented MVP skeleton
- Sibling Wave 1 plans can extend documented module boundaries without needing a structural rewrite
</success_criteria>

<output>
After completion, create `.planning/phases/01-market-discovery-foundation/01-01-SUMMARY.md`
</output>
