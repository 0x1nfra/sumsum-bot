---
phase: 01-market-discovery-foundation
plan: "05"
type: execute
wave: 5
depends_on:
  - "00"
  - "02"
  - "04"
files_modified:
  - config/settings.py
  - core/market_scanner.py
  - core/storage.py
  - strategies/weather/scanner.py
  - main.py
  - paper_trader.py
  - tests/unit/test_filter_rules.py
  - tests/integration/test_market_scan.py
autonomous: true
requirements:
  - DISC-01
  - DISC-02
user_setup: []
must_haves:
  truths:
    - Operators can run a scan continuously through a concrete entrypoint with interval control.
    - Accepted, review-required, and rejected candidates are filtered by configured discovery rules and persisted with explicit reasons and statuses.
    - Scanner output is inspectable and reusable by Phase 2 without re-parsing raw market data.
  artifacts:
    - config/settings.py
    - core/market_scanner.py
    - core/storage.py
    - main.py
    - paper_trader.py
    - tests/unit/test_filter_rules.py
    - tests/integration/test_market_scan.py
  key_links:
    - filter thresholds are read from config/settings.py, not hardcoded in scanner logic.
    - main.py or paper_trader.py exposes both scan-once and scan-loop behavior through the shared scanner.
    - scan execution persists accepted, review, and rejected candidate records through core/storage.py.
---

<objective>
Add candidate filtering rules, persisted scan outputs, and a concrete operator-facing continuous scan entrypoint.

Purpose: Complete Phase 1 by satisfying both discovery requirements with a scan flow that can run continuously, return auditable candidate decisions, and expose a usable command surface.
Output: Config-driven filtering logic, persisted scan runs, concrete `scan-once` and `scan-loop` entrypoint wiring, and tests covering accepted plus rejected candidate flows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-market-discovery-foundation/01-CONTEXT.md
@.planning/phases/01-market-discovery-foundation/01-RESEARCH.md
@.planning/phases/01-market-discovery-foundation/01-VALIDATION.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in This Plan |
|--------|----------|-------------------------|
| T-1-04 Filter thresholds are hardcoded or applied inconsistently | medium | Read all filter values from `config/settings.py` and cover them with unit tests. |
| T-1-05 Rejected candidates lose auditability | high | Persist explicit rule-level rejection reasons for liquidity, price, and resolution-window failures. |
| T-1-06 Continuous scanning loops forever without operator control | medium | Add explicit interval and optional iteration controls to the entrypoint and verify structured output from a bounded loop. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement config-driven candidate filter rules</name>
  <files>config/settings.py, strategies/weather/scanner.py, tests/unit/test_filter_rules.py</files>
  <read_first>config/settings.py, strategies/weather/scanner.py, .planning/REQUIREMENTS.md, .planning/phases/01-market-discovery-foundation/01-CONTEXT.md</read_first>
  <action>Extend `config/settings.py` so scanner thresholds explicitly include minimum liquidity, maximum entry price, maximum hours-to-resolution values, and a scan interval setting for continuous discovery. Update `strategies/weather/scanner.py` to apply those thresholds only after normalization succeeds, emitting exact rejection reasons such as `liquidity_below_threshold`, `price_above_ceiling`, and `resolution_window_too_wide`. Add `tests/unit/test_filter_rules.py` that verifies each threshold path independently and proves values come from settings rather than hardcoded constants.</action>
  <verify>python -m pytest tests/unit/test_filter_rules.py -q</verify>
  <acceptance_criteria>
    - `config/settings.py` contains liquidity, price ceiling, resolution-window, and scan-interval fields
    - `strategies/weather/scanner.py` contains explicit rejection reasons for each filter rule
    - `tests/unit/test_filter_rules.py` exits 0
    - filter-rule tests override settings values instead of depending only on defaults
  </acceptance_criteria>
  <done>Filtering and scan-loop configuration are explicit, deterministic, and test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Persist accepted, review, and rejected scan outputs with explicit statuses</name>
  <files>core/storage.py, core/market_scanner.py, tests/integration/test_market_scan.py</files>
  <read_first>core/storage.py, core/market_scanner.py, core/models.py, strategies/weather/scanner.py</read_first>
  <action>Update `core/storage.py` and `core/market_scanner.py` so every scan run stores accepted candidates, review-required candidates, and rejected candidates in the database, including candidate identifiers, normalization status, filter status, and explicit reason strings. Add `tests/integration/test_market_scan.py` using fixture market payloads and a temporary database to verify one candidate is accepted, one remains in review state, one is rejected for a filter rule, and all three records are persisted as part of the same scan run with distinct statuses.</action>
  <verify>python -m pytest tests/integration/test_market_scan.py -q</verify>
  <acceptance_criteria>
    - `core/market_scanner.py` persists a scan run through `core/storage.py`
    - `tests/integration/test_market_scan.py` exits 0
    - integration test asserts accepted, review, and rejected counts from one scan
    - integration test asserts persisted reason and status fields
  </acceptance_criteria>
  <done>Scan decisions become durable and auditable for later signal and dashboard work, including review-state markets.</done>
</task>

<task type="auto">
  <name>Task 3: Expose concrete scan-once and scan-loop entrypoints</name>
  <files>main.py, paper_trader.py, core/market_scanner.py</files>
  <read_first>main.py, paper_trader.py, core/market_scanner.py, config/settings.py, .planning/REQUIREMENTS.md</read_first>
  <action>Update `main.py` and `paper_trader.py` so at least one concrete operator-facing command supports a single scan (`scan-once`) and a bounded continuous scan loop (`scan-loop`) that reuses the shared scanner, respects the configured scan interval, and prints or returns structured output containing accepted, review, and rejected counts. Use the shared fixture file created in Wave 0 and include an execution mode such as `--iterations 2` so the continuous loop can be verified deterministically without running forever.</action>
  <verify>python main.py scan-once --fixture tests/fixtures/polymarket_weather_markets.json | tee /tmp/phase1-scan-once.out && rg -n \"accepted|review|rejected\" /tmp/phase1-scan-once.out && python main.py scan-loop --fixture tests/fixtures/polymarket_weather_markets.json --interval 0 --iterations 2 | tee /tmp/phase1-scan-loop.out && rg -n \"accepted|review|rejected|2\" /tmp/phase1-scan-loop.out</verify>
  <acceptance_criteria>
    - `main.py` or `paper_trader.py` contains `scan-once`
    - `main.py` or `paper_trader.py` contains `scan-loop`
    - the entrypoint wiring references `core.market_scanner`
    - the continuous scan command has interval and bounded-iteration control
  </acceptance_criteria>
  <done>Phase 1 ends with a usable operator-facing continuous scan surface for downstream signal evaluation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/unit/test_filter_rules.py -q`
- [ ] `python -m pytest tests/integration/test_market_scan.py -q`
- [ ] `rg -n "scan-once|scan-loop|iterations|interval" main.py paper_trader.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- An operator-facing entrypoint can run scans continuously with explicit interval control
- Accepted, review, and rejected outputs are durable and include explicit reason/status fields
</success_criteria>

<output>
After completion, create `.planning/phases/01-market-discovery-foundation/01-05-SUMMARY.md`
</output>
