---
phase: 02-noaa-signal-engine
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - strategies/weather/edge_calculator.py
  - tests/unit/test_weather_edge_calculator.py
autonomous: true
requirements: [WEAT-02]
must_haves:
  truths:
    - "Each supported contract family uses an explicit, reproducible probability rule instead of one shared heuristic."
    - "Temperature markets use the exact resolved threshold-margin bands from 02-RESEARCH.md, and each result stores the oriented margin plus selected band label."
    - "Signal acceptance is based on a documented edge threshold against the market NO price."
    - "Rejected evaluations expose concrete reason codes instead of null or implied math failures."
  artifacts:
    - path: "strategies/weather/edge_calculator.py"
      provides: "Contract-family evaluation logic and edge threshold checks"
    - path: "tests/unit/test_weather_edge_calculator.py"
      provides: "Red/green coverage for temperature and precipitation evaluators"
  key_links:
    - from: "strategies/weather/edge_calculator.py"
      to: "config/settings.py"
      via: "minimum_edge_to_trade threshold"
      pattern: "minimum_edge_to_trade"
    - from: "strategies/weather/edge_calculator.py"
      to: "strategies/weather/types.py"
      via: "NoaaForecastWindow and WeatherSignalInput inputs"
      pattern: "NoaaForecastWindow|WeatherSignalInput"
---

<objective>
Turn NOAA overlap evidence into reproducible signal math using separate evaluator paths for temperature and precipitation contracts.

Purpose: Satisfy D-03, D-04, and WEAT-02 by making the edge calculation explicit, testable, and explainable before persistence and orchestration are added.
Output: Unit-backed contract-family evaluators that produce derived weather probabilities, market-vs-NOAA edge, and deterministic acceptance or rejection reasons.
</objective>

<execution_context>
@/Users/irfanmurad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-noaa-signal-engine/02-CONTEXT.md
@.planning/phases/02-noaa-signal-engine/02-RESEARCH.md
@config/settings.py
@strategies/weather/types.py
@strategies/weather/edge_calculator.py
@strategies/weather/normalization.py
@.planning/phases/02-noaa-signal-engine/02-01-PLAN.md

<interfaces>
From Plan 02-01 contracts:
```python
@dataclass(frozen=True)
class WeatherSignalInput:
    market_id: str
    contract_family: str
    location_key: str
    threshold: float | None
    no_price: float
    market_date_local: date
    market_window_start_local: datetime
    market_window_end_local: datetime
```

Expected evaluation output shape for this plan:
```python
SignalEvaluation(
    accepted: bool,
    derived_yes_probability: float | None,
    derived_no_probability: float | None,
    edge_against_no_price: float | None,
    reason_codes: tuple[str, ...],
    evidence: dict[str, object],
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for temperature and precipitation evaluation rules</name>
  <read_first>
    - strategies/weather/edge_calculator.py
    - strategies/weather/types.py
    - config/settings.py
    - .planning/phases/02-noaa-signal-engine/02-PATTERNS.md
    - .planning/phases/02-noaa-signal-engine/02-RESEARCH.md
    - tests/unit/test_weather_normalization.py
    - strategies/weather/normalization.py
  </read_first>
  <files>tests/unit/test_weather_edge_calculator.py</files>
  <acceptance_criteria>
    - `rg -n "test_temperature_signal_accepts_when_noaa_no_edge_exceeds_threshold" tests/unit/test_weather_edge_calculator.py` returns one match.
    - `rg -n "test_precipitation_signal_rejects_when_overlap_points_are_missing" tests/unit/test_weather_edge_calculator.py` returns one match.
    - `rg -n "test_precipitation_signal_uses_pop_and_qpf_evidence" tests/unit/test_weather_edge_calculator.py` returns one match.
    - `rg -n "0.85|0.70|0.55|0.30|0.15|temperature_band_label|oriented_temperature_margin" tests/unit/test_weather_edge_calculator.py` returns matches for all seven patterns.
    - `rg -n "minimum_edge_to_trade|insufficient_forecast_overlap|edge_below_threshold" tests/unit/test_weather_edge_calculator.py` returns matches for all three strings.
  </acceptance_criteria>
  <action>Create `tests/unit/test_weather_edge_calculator.py` as red-first coverage for two concrete evaluator paths. Follow the transform-style unit-test shape mapped in `02-PATTERNS.md` from `tests/unit/test_weather_normalization.py`: explicit contract-family fixtures, deterministic assertions, and reason-code checks rather than snapshot-style output. The temperature cases must assert the exact resolved threshold-margin bands from `02-RESEARCH.md`: `>= 5.0 -> 0.85`, `>= 2.0 and < 5.0 -> 0.70`, `> -2.0 and < 2.0 -> 0.55`, `> -5.0 and <= -2.0 -> 0.30`, `<= -5.0 -> 0.15`, after orienting the margin so positive values favor the market's YES side. Require the returned evidence to include the raw overlapping temperature values, `oriented_temperature_margin`, and `temperature_band_label`. The temperature cases must also assert that the calculator accepts only when `derived_no_probability - no_price > minimum_edge_to_trade`. The precipitation cases must assert that PoP plus QPF overlap evidence is stored in the result and that missing overlap or insufficient NOAA data yields rejection codes `insufficient_forecast_overlap` or `noaa_payload_incomplete`. Include one acceptance case and one rejection case for each contract family, and assert the returned result exposes `derived_yes_probability`, `derived_no_probability`, `edge_against_no_price`, `reason_codes`, and evidence fields needed for later persistence.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_weather_edge_calculator.py -q</automated>
  </verify>
  <done>The unit spec file exists and pins one accepted and one rejected path for both temperature and precipitation evaluation, including the explicit edge threshold and reason codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement contract-family evaluators and edge gating</name>
  <read_first>
    - strategies/weather/edge_calculator.py
    - tests/unit/test_weather_edge_calculator.py
    - strategies/weather/types.py
    - config/settings.py
    - .planning/phases/02-noaa-signal-engine/02-PATTERNS.md
    - .planning/phases/02-noaa-signal-engine/02-CONTEXT.md
    - strategies/weather/normalization.py
  </read_first>
  <files>strategies/weather/edge_calculator.py</files>
  <acceptance_criteria>
    - `rg -n "class WeatherEdgeCalculator" strategies/weather/edge_calculator.py` returns one match.
    - `rg -n "_evaluate_temperature|_evaluate_precipitation" strategies/weather/edge_calculator.py` returns matches for both helpers.
    - `rg -n "edge_against_no_price|derived_no_probability|edge_below_threshold" strategies/weather/edge_calculator.py` returns matches for all three strings.
    - `rg -n "0.85|0.70|0.55|0.30|0.15|temperature_band_label|oriented_temperature_margin" strategies/weather/edge_calculator.py` returns matches for all seven patterns.
    - `rg -n "insufficient_forecast_overlap|unsupported_contract_family" strategies/weather/edge_calculator.py` returns matches for both rejection codes.
  </acceptance_criteria>
  <action>Replace the placeholder in `strategies/weather/edge_calculator.py` with a real `WeatherEdgeCalculator`, following the single-entrypoint plus contract-family branching analog mapped in `02-PATTERNS.md` from `strategies/weather/normalization.py`. Accept `WeatherSignalInput`, `NoaaForecastWindow`, and `ScanSettings`. Implement one public method that dispatches by `contract_family` and returns a structured evaluation result. Add a temperature helper `_evaluate_temperature(...)` that uses the exact resolved threshold-margin bands from `02-RESEARCH.md`: `>= 5.0 -> 0.85`, `>= 2.0 and < 5.0 -> 0.70`, `> -2.0 and < 2.0 -> 0.55`, `> -5.0 and <= -2.0 -> 0.30`, `<= -5.0 -> 0.15`, after orienting the forecast-vs-threshold margin so positive values favor the market's YES side. Persist the raw overlapping temperature evidence, the scalar `oriented_temperature_margin`, and the chosen `temperature_band_label` inside the evaluation evidence payload, then compute `derived_no_probability = 1.0 - derived_yes_probability` and `edge_against_no_price = derived_no_probability - signal_input.no_price`. Add a precipitation helper `_evaluate_precipitation(...)` that records overlapping PoP and QPF evidence from NOAA, derives a precipitation YES probability from that overlap, and computes the same NO-side edge. In both paths, set `accepted` only when `edge_against_no_price > settings.minimum_edge_to_trade`; otherwise return `accepted=False` with exact reason code `edge_below_threshold`. If no overlapping forecast evidence exists, return `accepted=False` with `insufficient_forecast_overlap`. If `contract_family` is neither `temperature` nor `precipitation`, return `accepted=False` with `unsupported_contract_family`. Per D-03 and D-04, do not collapse the two families into one shared probability formula.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_weather_edge_calculator.py -q</automated>
  </verify>
  <done>The edge calculator is no longer a placeholder; it has explicit temperature and precipitation paths, computes the documented NO-side edge threshold, and returns deterministic acceptance/rejection results with passing unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NOAA overlap evidence -> edge calculator | External forecast evidence is transformed into trade eligibility |
| settings -> edge gate | Configured thresholds influence whether a trade becomes eligible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `strategies/weather/edge_calculator.py` | mitigate | Dispatch by exact `contract_family` and reject unsupported families rather than applying one generic rule |
| T-02-05 | R | `strategies/weather/edge_calculator.py` | mitigate | Return structured evidence fields and explicit `reason_codes` for both accepted and rejected paths |
| T-02-06 | E | `strategies/weather/edge_calculator.py` | accept | Temperature probability remains a conservative proxy in v1; unit tests and persisted evidence make the assumption inspectable rather than hidden |
</threat_model>

<verification>
Run `uv run --extra dev pytest tests/unit/test_weather_edge_calculator.py -q`.
Run `uv run --extra dev pytest tests/unit -q` after completion to keep feedback latency inside the Phase 2 validation target.
</verification>

<success_criteria>
Temperature and precipitation candidates are scored by separate evaluator paths, temperature scoring uses the exact resolved margin bands with persisted band evidence, and acceptance requires the exact configured NO-side edge threshold.
</success_criteria>

<output>
After completion, create `.planning/phases/02-noaa-signal-engine/02-noaa-signal-engine-02-SUMMARY.md`
</output>
