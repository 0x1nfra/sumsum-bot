---
phase: 02-noaa-signal-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.py
  - config/weather_locations.py
  - core/models.py
  - core/storage.py
  - strategies/weather/types.py
  - strategies/weather/noaa_client.py
  - tests/unit/test_noaa_client.py
autonomous: true
requirements: [WEAT-01]
must_haves:
  truths:
    - "Supported weather candidates resolve to one explicit NOAA city mapping and one explicit local contract window."
    - "NOAA requests use the official points-to-grid workflow and refuse stale, incomplete, or mismatched forecast coverage."
    - "Unsupported or ambiguous cities never fall through to guessed coordinates."
  artifacts:
    - path: "config/weather_locations.py"
      provides: "Curated city-to-lat-lon-timezone mapping table for supported weather markets"
    - path: "strategies/weather/noaa_client.py"
      provides: "NOAA points/grid forecast client with hard rejection paths"
    - path: "strategies/weather/types.py"
      provides: "Signal input and NOAA evidence dataclasses preserving local contract-day data"
  key_links:
    - from: "config/weather_locations.py"
      to: "strategies/weather/noaa_client.py"
      via: "location key -> lat/lon/timezone lookup"
      pattern: "SUPPORTED_WEATHER_LOCATIONS|WeatherLocationMapping"
    - from: "strategies/weather/types.py"
      to: "strategies/weather/noaa_client.py"
      via: "WeatherSignalInput passed into NOAA fetch methods"
      pattern: "WeatherSignalInput"
---

<objective>
Preserve the local contract-day context that Phase 1 currently drops, then implement the curated NOAA mapping and forecast retrieval boundary required to score supported weather candidates.

Purpose: Satisfy D-01, D-02, and D-05 by making NOAA access deterministic, fail-closed, and contract-window-aware before any signal math is introduced.
Output: Typed NOAA/signal input contracts, curated city mapping, a real NOAA client, and unit coverage for points/grid fetch plus rejection paths.
</objective>

<execution_context>
@/Users/irfanmurad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-noaa-signal-engine/02-CONTEXT.md
@.planning/phases/02-noaa-signal-engine/02-RESEARCH.md
@.planning/phases/02-noaa-signal-engine/02-PATTERNS.md
@config/settings.py
@strategies/weather/types.py
@strategies/weather/normalization.py
@strategies/weather/noaa_client.py
@tests/fixtures/polymarket_weather_markets.json

<interfaces>
From strategies/weather/types.py:
```python
@dataclass(frozen=True)
class WeatherMarketCandidate:
    market_id: str
    title: str
    status: CandidateStatus
    contract_family: str | None
    location: str
    threshold: float | None
    unit: str | None
    resolution_at: datetime | None
    no_price: float
    liquidity_usd: float
    reason_codes: tuple[str, ...] = field(default_factory=tuple)
```

From core/models.py (must be extended in this plan per the resolved research handoff decision):
```python
@dataclass(frozen=True)
class CandidateRecord:
    market_id: str
    title: str
    status: CandidateStatus
    location: str
    metric: str | None = None
    region: str | None = None
    threshold: float | None = None
    unit: str | None = None
    no_price: float | None = None
    liquidity_usd: float | None = None
    resolution_hours: int | None = None
    market_date_local: str | None = None
    market_window_start_local: str | None = None
    market_window_end_local: str | None = None
    location_key: str | None = None
    normalization_status: CandidateStatus | None = None
    rejection_reasons: tuple[str, ...] = field(default_factory=tuple)
```

From config/settings.py:
```python
@dataclass(frozen=True)
class ScanSettings:
    database_path: str = "data/trades.db"
    min_market_liquidity_usd: int = 5000
    max_entry_price: float = 0.85
    max_resolution_hours: int = 72
    scan_interval_seconds: int = 300
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NOAA client contract tests and fixture-backed rejection cases</name>
  <read_first>
    - tests/unit/test_weather_normalization.py
    - tests/fixtures/polymarket_weather_markets.json
    - .planning/phases/02-noaa-signal-engine/02-PATTERNS.md
    - strategies/weather/noaa_client.py
    - strategies/weather/types.py
    - core/clob_client.py
  </read_first>
  <files>tests/unit/test_noaa_client.py</files>
  <acceptance_criteria>
    - `rg -n "test_points_lookup_uses_curated_city_mapping" tests/unit/test_noaa_client.py` returns one match.
    - `rg -n "test_fetch_forecast_window_rejects_stale_grid_data" tests/unit/test_noaa_client.py` returns one match.
    - `rg -n "test_fetch_forecast_window_rejects_missing_overlap" tests/unit/test_noaa_client.py` returns one match.
    - `rg -n "WeatherSignalInput|NoaaDataError|noaa_data_stale|noaa_window_mismatch|noaa_city_unsupported|noaa_payload_incomplete" tests/unit/test_noaa_client.py` returns matches for all six symbols/strings.
  </acceptance_criteria>
  <action>Create `tests/unit/test_noaa_client.py` with red-first unit coverage for the real Phase 2 NOAA contract. Follow the request/response test shape mapped in `02-PATTERNS.md` from `tests/unit/test_weather_normalization.py` and the client-boundary/error pattern mapped from `core/clob_client.py`: one test module, deterministic fixtures, explicit malformed/stale rejection cases, and no NOAA parsing spread into other modules. The test file must define fixture-driven cases for: `lookup_points()` resolving a supported city from a curated mapping table; `fetch_forecast_window(signal_input)` following the `https://api.weather.gov/points/{lat},{lon}` -> `forecastGridData` flow; hard rejection when NOAA `updateTime` is older than the configured freshness threshold; hard rejection when `validTime` intervals do not overlap the signal window; hard rejection when a requested city key is absent from the curated map; and hard rejection when required NOAA payload fields are missing, per D-01, D-02, and D-05. Use concrete symbols in the tests: `WeatherSignalInput`, `NoaaForecastWindow`, `NoaaDataError`, and the exact lowercase rejection codes `noaa_city_unsupported`, `noaa_data_stale`, `noaa_window_mismatch`, and `noaa_payload_incomplete`.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_noaa_client.py -q</automated>
  </verify>
  <done>Unit tests exist and fail only until the NOAA mapping/client implementation is added; they explicitly pin supported-city lookup, freshness rejection, and overlap rejection behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend settings and define explicit supported-city + signal-input contracts</name>
  <read_first>
    - config/settings.py
    - core/models.py
    - core/storage.py
    - strategies/weather/types.py
    - strategies/weather/normalization.py
    - .planning/phases/02-noaa-signal-engine/02-PATTERNS.md
    - .planning/phases/02-noaa-signal-engine/02-CONTEXT.md
    - .planning/phases/02-noaa-signal-engine/02-RESEARCH.md
  </read_first>
  <files>config/settings.py, config/weather_locations.py, core/models.py, core/storage.py, strategies/weather/types.py</files>
  <acceptance_criteria>
    - `rg -n "noaa_user_agent|noaa_request_timeout_seconds|noaa_max_data_age_minutes|minimum_edge_to_trade" config/settings.py` returns all four fields.
    - `rg -n "class WeatherLocationMapping" config/weather_locations.py` returns one match.
    - `rg -n "SUPPORTED_WEATHER_LOCATIONS" config/weather_locations.py` returns one match.
    - `rg -n "market_date_local|market_window_start_local|market_window_end_local|location_key" core/models.py` returns matches for all four fields.
    - `rg -n "market_date_local|market_window_start_local|market_window_end_local|location_key" core/storage.py` returns matches for all four persisted fields.
    - `rg -n "market_date_local|market_window_start_local|market_window_end_local|location_key" strategies/weather/types.py` returns matches for all four fields.
  </acceptance_criteria>
  <action>Modify `config/settings.py` and `config/weather_locations.py` using the frozen-dataclass config pattern mapped in `02-PATTERNS.md` from `config/settings.py`: typed settings only, deterministic config data only, no lookup I/O in config modules. Extend `ScanSettings` with concrete NOAA/evaluation fields: `noaa_user_agent: str = "(sumsum-bot, contact@example.com)"`, `noaa_request_timeout_seconds: float = 10.0`, `noaa_max_data_age_minutes: int = 180`, and `minimum_edge_to_trade: float = 0.10`. Update `load_settings()` so overrides/env vars `NOAA_USER_AGENT`, `NOAA_REQUEST_TIMEOUT_SECONDS`, `NOAA_MAX_DATA_AGE_MINUTES`, and `MINIMUM_EDGE_TO_TRADE` map into those exact fields. Add `config/weather_locations.py` with a frozen dataclass `WeatherLocationMapping(city_key: str, market_label: str, latitude: float, longitude: float, timezone: str)` and a deterministic `SUPPORTED_WEATHER_LOCATIONS` mapping keyed by normalized city names taken from current fixtures, plus a helper `get_weather_location(city_key: str) -> WeatherLocationMapping | None`. Extend the shared scanner/storage contract in `core/models.py` and `core/storage.py` so approved `CandidateRecord` rows persist and round-trip `location_key`, `market_date_local`, `market_window_start_local`, and `market_window_end_local` directly from normalization/scanner output; follow the shared-record and repository-schema analogs mapped in `02-PATTERNS.md` from `core/models.py` and `core/storage.py`. Extend `strategies/weather/types.py` with Phase 2 contracts that consume that shared handoff without reparsing raw payloads late in the pipeline: `WeatherSignalInput`, `NoaaForecastPoint`, `NoaaForecastWindow`, and helper constructors that build a signal input from an approved `CandidateRecord` carrying those four explicit fields. Do not rely on `resolution_hours` alone anywhere in these contracts; per D-01 and the resolved research decision, `market_date_local`, `market_window_start_local`, and `market_window_end_local` are the authoritative Phase 2 handoff.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_noaa_client.py -q</automated>
  </verify>
  <done>The settings surface exposes NOAA freshness/timeout/edge keys, the city mapping table exists as deterministic config, and the typed signal-input contract preserves the exact local market window for downstream evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the NOAA points and forecast-window client with hard rejection paths</name>
  <read_first>
    - strategies/weather/noaa_client.py
    - config/weather_locations.py
    - strategies/weather/types.py
    - .planning/phases/02-noaa-signal-engine/02-PATTERNS.md
    - tests/unit/test_noaa_client.py
    - .planning/phases/02-noaa-signal-engine/02-RESEARCH.md
    - core/clob_client.py
  </read_first>
  <files>strategies/weather/noaa_client.py</files>
  <acceptance_criteria>
    - `rg -n "class NoaaDataError" strategies/weather/noaa_client.py` returns one match.
    - `rg -n "lookup_points\\(" strategies/weather/noaa_client.py` returns one match.
    - `rg -n "fetch_forecast_window\\(" strategies/weather/noaa_client.py` returns one match.
    - `rg -n "noaa_city_unsupported|noaa_data_stale|noaa_window_mismatch|forecastGridData" strategies/weather/noaa_client.py` returns matches for all four strings.
  </acceptance_criteria>
  <action>Replace the placeholder in `strategies/weather/noaa_client.py` with a real NOAA client built around Python stdlib HTTP only, using `urllib.request`/`urllib.error` (not `httpx` and no new dependency work), while following the dedicated-client, explicit-error, and validate-before-normalize analog mapped in `02-PATTERNS.md` from `core/clob_client.py`. Implement `NoaaDataError(ValueError)` plus a client class that reads `ScanSettings`, uses the exact `User-Agent` and timeout fields from Task 2, resolves `WeatherSignalInput.location_key` through `get_weather_location()`, calls `https://api.weather.gov/points/{lat},{lon}`, extracts `properties.forecastGridData`, fetches the gridpoint payload, expands the NOAA `validTime` intervals that overlap `market_window_start_local` through `market_window_end_local`, and returns a `NoaaForecastWindow` containing `update_time`, `forecast_source_url`, and the overlapping points needed later by the edge calculator. Consume the explicit local-window fields from the shared handoff introduced in Task 2 rather than reconstructing dates from raw titles or payload metadata. If the city is unsupported, `updateTime` is older than `noaa_max_data_age_minutes`, the payload is malformed, or there is no interval overlap with the contract window, raise `NoaaDataError` with the exact lowercase rejection codes `noaa_city_unsupported`, `noaa_data_stale`, `noaa_payload_incomplete`, or `noaa_window_mismatch` per D-02, D-05, and D-06.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_noaa_client.py -q</automated>
  </verify>
  <done>The NOAA client is no longer a placeholder; it resolves only supported cities, uses the official points-to-grid flow, preserves overlapping forecast evidence, and raises explicit hard-rejection codes for every unsupported or stale path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| weather candidate -> NOAA client | Parsed market data becomes remote NOAA lookup parameters and window constraints |
| NOAA API -> local evaluator | Untrusted external forecast payloads cross into local scoring logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `strategies/weather/noaa_client.py` | mitigate | Validate supported city keys through `SUPPORTED_WEATHER_LOCATIONS`; never derive coordinates from free text |
| T-02-02 | I | `strategies/weather/noaa_client.py` | mitigate | Reject stale or malformed NOAA payloads using `updateTime` checks and explicit `NoaaDataError` reason codes |
| T-02-03 | D | `strategies/weather/noaa_client.py` | accept | Single-provider NOAA downtime remains possible in v1; surface deterministic rejection codes so the runtime can fail closed without trading |
</threat_model>

<verification>
Run `uv run --extra dev pytest tests/unit/test_noaa_client.py -q`.
Run `uv run --extra dev pytest tests/unit -q` after the plan completes to confirm the new NOAA contract did not break Phase 1 unit coverage.
</verification>

<success_criteria>
`WeatherSignalInput` preserves explicit local market-window fields, `SUPPORTED_WEATHER_LOCATIONS` covers only approved fixture cities, and the NOAA client enforces fail-closed city lookup plus stale/mismatch rejection with passing unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-noaa-signal-engine/02-noaa-signal-engine-01-SUMMARY.md`
</output>
