---
phase: 02-noaa-signal-engine
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - core/models.py
  - core/storage.py
  - strategies/weather/signal_engine.py
  - tests/integration/test_signal_storage.py
  - tests/integration/test_noaa_signal_engine.py
autonomous: true
requirements: [WEAT-01, WEAT-03]
must_haves:
  truths:
    - "Every accepted and rejected signal evaluation is durably stored with mapping inputs, market price, forecast evidence, derived probabilities, edge, and explicit reasons."
    - "Running the Phase 2 signal engine turns approved scanner candidates into inspectable evaluations without reparsing raw market payloads."
    - "Signal history is append-only so repeated evaluations do not overwrite prior audit evidence."
  artifacts:
    - path: "core/storage.py"
      provides: "Append-only signal evaluation persistence"
    - path: "strategies/weather/signal_engine.py"
      provides: "Candidate -> NOAA -> edge -> storage orchestration"
    - path: "tests/integration/test_noaa_signal_engine.py"
      provides: "End-to-end proof that accepted and rejected outcomes remain inspectable"
  key_links:
    - from: "strategies/weather/signal_engine.py"
      to: "strategies/weather/noaa_client.py"
      via: "fetch_forecast_window()"
      pattern: "fetch_forecast_window"
    - from: "strategies/weather/signal_engine.py"
      to: "strategies/weather/edge_calculator.py"
      via: "calculate/evaluate call"
      pattern: "WeatherEdgeCalculator"
    - from: "strategies/weather/signal_engine.py"
      to: "core/storage.py"
      via: "persist_signal_evaluations"
      pattern: "signal_evaluations"
---

<objective>
Wire NOAA retrieval and contract-family scoring into a durable signal-evaluation pipeline with append-only audit storage.

Purpose: Satisfy D-07, D-08, and WEAT-03 while keeping Phase 2 evidence separate from the Phase 1 latest-state candidate buckets.
Output: Integration-backed signal storage schema, append-only repository methods, and a real `SignalEngine` that evaluates approved candidates and persists both accepted and rejected outcomes.
</objective>

<execution_context>
@/Users/irfanmurad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-noaa-signal-engine/02-CONTEXT.md
@.planning/phases/02-noaa-signal-engine/02-RESEARCH.md
@core/models.py
@core/storage.py
@core/market_scanner.py
@strategies/weather/noaa_client.py
@strategies/weather/edge_calculator.py
@tests/integration/test_storage.py
@tests/integration/test_market_scan.py

<interfaces>
From core/storage.py:
```python
class CandidateStorage:
    def persist_scan_result(self, source: str, candidates: Iterable[CandidateRecord]) -> int: ...
    def list_candidates(self, status: CandidateStatus) -> list[CandidateRecord]: ...
```

From Plan 02-01 shared handoff (must already exist before this plan starts):
```python
@dataclass(frozen=True)
class CandidateRecord:
    market_id: str
    title: str
    status: CandidateStatus
    location: str
    contract_family: str | None
    threshold: float | None
    no_price: float | None
    market_date_local: str | None
    market_window_start_local: str | None
    market_window_end_local: str | None
    location_key: str | None
```

Expected new persistence contract for this plan:
```python
persist_signal_evaluations(source: str, evaluations: Iterable[SignalEvaluationRecord]) -> int
```

Expected persisted record shape for this plan:
```python
@dataclass(frozen=True)
class SignalEvaluationRecord:
    market_id: str
    status: SignalEvaluationStatus
    no_price: float
    mapping_city_key: str
    forecast_update_time: str | None
    forecast_source_url: str | None
    derived_yes_probability: float | None
    derived_no_probability: float | None
    edge_against_no_price: float | None
    decision_reason: str
    evidence: dict[str, object]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration tests for append-only signal storage and end-to-end signal evaluation</name>
  <read_first>
    - tests/integration/test_storage.py
    - tests/integration/test_market_scan.py
    - .planning/phases/02-noaa-signal-engine/02-PATTERNS.md
    - core/storage.py
    - core/market_scanner.py
    - strategies/weather/noaa_client.py
    - strategies/weather/edge_calculator.py
  </read_first>
  <files>tests/integration/test_signal_storage.py, tests/integration/test_noaa_signal_engine.py</files>
  <acceptance_criteria>
    - `rg -n "test_signal_storage_appends_accepted_and_rejected_evaluations" tests/integration/test_signal_storage.py` returns one match.
    - `rg -n "test_signal_engine_persists_mapping_forecast_edge_and_reason_fields" tests/integration/test_noaa_signal_engine.py` returns one match.
    - `rg -n "signal_evaluations|status|mapping_city_key|forecast_update_time|no_price|edge_against_no_price|decision_reason" tests/integration/test_signal_storage.py tests/integration/test_noaa_signal_engine.py` returns matches for all seven strings.
  </acceptance_criteria>
  <action>Create two integration specs using the CRUD and thin-orchestrator test shapes mapped in `02-PATTERNS.md` from `tests/integration/test_storage.py` and `tests/integration/test_market_scan.py`. In `tests/integration/test_signal_storage.py`, verify that bootstrapping storage creates a new append-only `signal_evaluations` table and that inserting one accepted plus one rejected evaluation preserves two rows for the same `market_id` across repeated runs. Assert the persisted contract uses `status` with exact values `accepted` and `rejected`; do not introduce or expect a persisted `accepted` boolean column. In `tests/integration/test_noaa_signal_engine.py`, verify that a real signal-engine run consumes approved candidates from the scanner/storage layer with pre-populated `location_key`, `market_date_local`, `market_window_start_local`, and `market_window_end_local` fields, maps the city through the curated table, persists `mapping_city_key`, `forecast_update_time`, `forecast_source_url`, `no_price`, `derived_yes_probability`, `derived_no_probability`, `edge_against_no_price`, `status`, and `decision_reason`, and stores rejection evidence when NOAA data is stale or edge is below threshold. The test must fail if the engine tries to reconstruct these fields from raw payloads or titles. Use deterministic fake NOAA payloads or injected client stubs so the test is repeatable.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_signal_storage.py tests/integration/test_noaa_signal_engine.py -q</automated>
  </verify>
  <done>The integration specs exist and pin the append-only storage requirement plus the full accepted/rejected signal-evaluation evidence set before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend shared models and storage with append-only signal evaluation records</name>
  <read_first>
    - core/models.py
    - core/storage.py
    - .planning/phases/02-noaa-signal-engine/02-PATTERNS.md
    - tests/integration/test_signal_storage.py
    - .planning/phases/02-noaa-signal-engine/02-CONTEXT.md
  </read_first>
  <files>core/models.py, core/storage.py</files>
  <acceptance_criteria>
    - `rg -n "class SignalEvaluationStatus|class SignalEvaluationRecord" core/models.py` returns matches for both symbols.
    - `rg -n "CREATE TABLE IF NOT EXISTS signal_evaluations" core/storage.py` returns one match.
    - `rg -n "persist_signal_evaluations|list_signal_evaluations" core/storage.py` returns matches for both methods.
    - `rg -n "mapping_city_key|forecast_update_time|no_price|edge_against_no_price|decision_reason|status|evidence_json" core/storage.py` returns matches for all seven column names.
  </acceptance_criteria>
  <action>Modify `core/models.py` and `core/storage.py` using the shared-record and repository-schema analogs mapped in `02-PATTERNS.md`. Add shared signal vocabulary: `SignalEvaluationStatus` with exact values `accepted` and `rejected`, plus a frozen `SignalEvaluationRecord` that stores `market_id`, `scan_run_id`, `location`, `mapping_city_key`, `contract_family`, `market_date_local`, `market_window_start_local`, `market_window_end_local`, `forecast_update_time`, `forecast_source_url`, `no_price`, `derived_yes_probability`, `derived_no_probability`, `edge_against_no_price`, `decision_reason`, `status`, and structured `evidence`. Preserve the explicit local-window fields introduced on `CandidateRecord` in Plan `02-01`; this plan may reuse them but must not redefine or reconstruct them from raw payload text. Extend `core/storage.py` so `bootstrap()` creates an append-only `signal_evaluations` table with those exact columns plus `evaluated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`. The schema and repository contract must persist state via the `status` enum only; do not add or imply any persisted `accepted` boolean column. Add repository methods `persist_signal_evaluations(source: str, evaluations: Iterable[SignalEvaluationRecord]) -> int` and `list_signal_evaluations(market_id: str | None = None) -> list[SignalEvaluationRecord]`. Do not upsert these rows into the Phase 1 latest-state bucket tables; per D-07 and D-08, every evaluation must remain queryable as history.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_signal_storage.py -q</automated>
  </verify>
  <done>Signal evaluation history has its own append-only schema and repository methods, with shared models that preserve all operator-inspection fields required by WEAT-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the signal engine that evaluates approved candidates and persists both outcomes</name>
  <read_first>
    - strategies/weather/noaa_client.py
    - strategies/weather/edge_calculator.py
    - .planning/phases/02-noaa-signal-engine/02-PATTERNS.md
    - core/market_scanner.py
    - core/storage.py
    - tests/integration/test_noaa_signal_engine.py
  </read_first>
  <files>strategies/weather/signal_engine.py</files>
  <acceptance_criteria>
    - `rg -n "class SignalEngine" strategies/weather/signal_engine.py` returns one match.
    - `rg -n "evaluate_candidates\\(|persist_signal_evaluations\\(" strategies/weather/signal_engine.py` returns matches for both methods/calls.
    - `rg -n "mapping_city_key|forecast_source_url|no_price|edge_against_no_price|decision_reason|status" strategies/weather/signal_engine.py` returns matches for all six strings.
    - `rg -n "NoaaDataError" strategies/weather/signal_engine.py` returns one match.
  </acceptance_criteria>
  <action>Create `strategies/weather/signal_engine.py` as a thin orchestrator modeled on the injected-dependency and frozen-handoff pattern mapped in `02-PATTERNS.md` from `core/market_scanner.py`. It must accept injected `NoaaForecastClient`, `WeatherEdgeCalculator`, `CandidateStorage`, and `ScanSettings`; read approved candidates from the scanner output or `CandidateStorage.list_candidates(CandidateStatus.APPROVED)` without reparsing raw payloads; require that each consumed `CandidateRecord` already carries `location_key`, `market_date_local`, `market_window_start_local`, `market_window_end_local`, and `no_price`; convert each approved `CandidateRecord` into a `WeatherSignalInput`; call `fetch_forecast_window()` and then the edge calculator; convert both NOAA hard rejections and edge-calculator rejections into `SignalEvaluationRecord` rows with `status="rejected"`; convert accepted results into `status="accepted"` rows; and persist the full batch through `persist_signal_evaluations()`. Persist `no_price` alongside forecast and probability evidence on every row, and use the `status` enum only rather than any persisted `accepted` boolean. Ensure the persisted `decision_reason` is populated from the first rejection code or the literal string `edge_threshold_passed` for accepted rows. Keep the entrypoint thin and deterministic: orchestration belongs here, but NOAA parsing and probability math must remain inside their dedicated modules.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_signal_storage.py tests/integration/test_noaa_signal_engine.py -q</automated>
  </verify>
  <done>The signal engine can evaluate approved candidates end to end, persist accepted and rejected signal rows with full evidence, and leave the Phase 1 candidate buckets untouched as latest-state discovery summaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner output -> signal engine | Approved candidate records become tradable signal inputs |
| signal engine -> storage | Derived decisions and external evidence are written into durable audit storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | R | `core/storage.py` | mitigate | Use append-only `signal_evaluations` rows with `evaluated_at`, explicit `decision_reason`, and structured evidence JSON |
| T-02-08 | T | `strategies/weather/signal_engine.py` | mitigate | Catch `NoaaDataError` and persist rejection rows instead of dropping failed evaluations silently |
| T-02-09 | D | `core/storage.py` | accept | SQLite remains a single-node durability boundary in v1; the plan keeps the schema PostgreSQL-ready without introducing migration scope now |
</threat_model>

<verification>
Run `uv run --extra dev pytest tests/integration/test_signal_storage.py tests/integration/test_noaa_signal_engine.py -q`.
Run `uv run --extra dev pytest tests/unit tests/integration -q` after the plan completes for wave-level validation.
</verification>

<success_criteria>
The repo contains a dedicated append-only `signal_evaluations` history, and a signal-engine run stores both accepted and rejected evaluations with `status`, `no_price`, mapping, forecast, probability, edge, and reason fields intact.
</success_criteria>

<output>
After completion, create `.planning/phases/02-noaa-signal-engine/02-noaa-signal-engine-03-SUMMARY.md`
</output>
