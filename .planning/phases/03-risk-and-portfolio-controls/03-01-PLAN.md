---
phase: 03-risk-and-portfolio-controls
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.py
  - config/kill_switches.py
  - core/models.py
  - core/kelly_engine.py
  - core/risk_manager.py
  - tests/unit/test_kelly_engine.py
  - tests/unit/test_risk_manager.py
autonomous: true
requirements: [RISK-01]
must_haves:
  truths:
    - "Position sizing starts from accepted Phase 2 signal probabilities, applies an explicit conservative haircut, and uses quarter-Kelly rather than full Kelly."
    - "No proposed trade can exceed 5% of current bankroll, remaining same-window exposure headroom, or remaining global exposure headroom."
    - "Trades below the minimum practical stake are blocked with explicit rule codes instead of silently rounding to zero."
    - "The risk layer preserves current-bankroll sizing and peak-bankroll drawdown semantics as separate concepts."
  artifacts:
    - path: "core/kelly_engine.py"
      provides: "Deterministic Kelly sizing and clamp evidence"
    - path: "core/risk_manager.py"
      provides: "Exposure-policy evaluation over portfolio snapshots and sizing decisions"
    - path: "tests/unit/test_kelly_engine.py"
      provides: "Regression coverage for haircut, Kelly fraction, caps, and undersized-trade blocks"
  key_links:
    - from: "core/kelly_engine.py"
      to: "core/risk_manager.py"
      via: "KellySizingDecision consumed by exposure-policy evaluation"
      pattern: "KellySizingDecision"
    - from: "config/settings.py"
      to: "core/kelly_engine.py"
      via: "risk thresholds loaded from typed settings"
      pattern: "kelly_fraction|per_trade_exposure_cap_pct|minimum_trade_stake_usd"
---

<objective>
Establish the deterministic Phase 3 policy core: typed risk settings, immutable portfolio and sizing records, explicit Kelly math with haircut plus hard caps, and exposure-policy evaluation that can explain every clamp or early block.

Purpose: satisfy the sizing half of RISK-01 before persistence or signal-integration work begins.
Output: concrete risk configuration fields, a real Kelly engine, a non-placeholder risk manager for sizing and exposure policy, and passing unit coverage for clamp behavior and block reasons.
</objective>

<execution_context>
@/Users/irfanmurad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-risk-and-portfolio-controls/03-CONTEXT.md
@.planning/phases/03-risk-and-portfolio-controls/03-RESEARCH.md
@.planning/phases/03-risk-and-portfolio-controls/03-PATTERNS.md
@config/settings.py
@config/kill_switches.py
@core/models.py
@core/kelly_engine.py
@core/risk_manager.py
@strategies/weather/edge_calculator.py
@strategies/weather/signal_engine.py
@tests/unit/test_weather_edge_calculator.py
@tests/integration/test_noaa_signal_engine.py

<interfaces>
From strategies/weather/edge_calculator.py:
```python
@dataclass(frozen=True)
class SignalEvaluation:
    accepted: bool
    derived_yes_probability: float | None
    derived_no_probability: float | None
    edge_against_no_price: float | None
    reason_codes: tuple[str, ...]
    evidence: dict[str, object]
```

From core/models.py:
```python
@dataclass(frozen=True)
class SignalEvaluationRecord:
    market_id: str
    scan_run_id: int | None
    location: str
    mapping_city_key: str
    contract_family: str | None
    ...
```

From config/kill_switches.py:
```python
@dataclass(frozen=True)
class KillSwitchSettings:
    max_drawdown_pct: float = 0.20
    max_loss_streak: int = 5
    pause_minutes_after_trigger: int = 60
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red-first unit coverage for Kelly sizing and exposure-policy outcomes</name>
  <read_first>
    - core/kelly_engine.py
    - core/risk_manager.py
    - core/models.py
    - config/settings.py
    - config/kill_switches.py
    - tests/unit/test_weather_edge_calculator.py
    - tests/integration/test_noaa_signal_engine.py
    - .planning/phases/03-risk-and-portfolio-controls/03-RESEARCH.md
  </read_first>
  <files>tests/unit/test_kelly_engine.py, tests/unit/test_risk_manager.py</files>
  <acceptance_criteria>
    - `rg -n "test_kelly_engine_applies_probability_haircut_before_quarter_kelly" tests/unit/test_kelly_engine.py` returns one match.
    - `rg -n "test_kelly_engine_caps_stake_by_per_trade_limit" tests/unit/test_kelly_engine.py` returns one match.
    - `rg -n "test_kelly_engine_blocks_stakes_below_minimum_practical_size" tests/unit/test_kelly_engine.py` returns one match.
    - `rg -n "test_risk_manager_blocks_when_global_exposure_headroom_is_exhausted" tests/unit/test_risk_manager.py` returns one match.
    - `rg -n "test_risk_manager_blocks_when_window_exposure_headroom_is_exhausted" tests/unit/test_risk_manager.py` returns one match.
    - `rg -n "test_risk_manager_returns_allowed_stake_when_policy_limits_do_not_block" tests/unit/test_risk_manager.py` returns one match.
    - `rg -n "per_trade_cap_exceeded|window_exposure_cap_exceeded|global_exposure_cap_exceeded|stake_below_minimum" tests/unit/test_risk_manager.py tests/unit/test_kelly_engine.py` returns matches for all four strings.
  </acceptance_criteria>
  <action>Create `tests/unit/test_kelly_engine.py` and `tests/unit/test_risk_manager.py` using the repo's existing pure-logic unit-test style. `test_kelly_engine.py` must pin these exact behaviors against a deterministic bankroll and `derived_no_probability`: apply `probability_haircut_pct` before Kelly math, convert the result to quarter-Kelly, clamp stake by `per_trade_exposure_cap_pct=0.05`, and block with the exact rule code `stake_below_minimum` when the final stake drops below `minimum_trade_stake_usd`. `test_risk_manager.py` must construct explicit `PortfolioSnapshot` and `SignalEvaluationRecord` inputs and assert exact rule-code outputs for `global_exposure_cap_exceeded`, `window_exposure_cap_exceeded`, and a passing approval path that returns a nonzero `allowed_stake_usd`. Use explicit numeric assertions for `proposed_stake_usd`, `allowed_stake_usd`, and the current-vs-peak bankroll fields rather than qualitative checks.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_kelly_engine.py tests/unit/test_risk_manager.py -q</automated>
  </verify>
  <done>Unit tests exist and fail only until Phase 3 sizing and exposure policy are implemented; they pin the exact haircut, clamp, and rule-code behavior required by RISK-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend typed risk configuration and shared risk vocabulary</name>
  <read_first>
    - config/settings.py
    - config/kill_switches.py
    - core/models.py
    - tests/unit/test_settings.py
    - .planning/phases/03-risk-and-portfolio-controls/03-CONTEXT.md
    - .planning/phases/03-risk-and-portfolio-controls/03-RESEARCH.md
  </read_first>
  <files>config/settings.py, config/kill_switches.py, core/models.py, tests/unit/test_settings.py</files>
  <acceptance_criteria>
    - `rg -n "kelly_fraction|probability_haircut_pct|per_trade_exposure_cap_pct|global_max_open_exposure_pct|window_max_open_exposure_pct|minimum_trade_stake_usd" config/settings.py` returns matches for all six fields.
    - `rg -n "max_drawdown_pct|cooldown_minutes|block_unaccepted_signals" config/kill_switches.py` returns matches for all three strings.
    - `rg -n "class PortfolioSnapshot" core/models.py` returns one match.
    - `rg -n "class RiskDecisionStatus" core/models.py` returns one match.
    - `rg -n "class RiskDecisionRecord" core/models.py` returns one match.
    - `rg -n "test_scan_settings_loads_risk_fields_from_env" tests/unit/test_settings.py` returns one match.
  </acceptance_criteria>
  <action>Extend `ScanSettings` in `config/settings.py` with these exact typed fields and defaults: `kelly_fraction: float = 0.25`, `probability_haircut_pct: float = 0.05`, `per_trade_exposure_cap_pct: float = 0.05`, `global_max_open_exposure_pct: float = 0.30`, `window_max_open_exposure_pct: float = 0.15`, and `minimum_trade_stake_usd: float = 1.00`. Update `load_settings()` to map environment overrides `KELLY_FRACTION`, `PROBABILITY_HAIRCUT_PCT`, `PER_TRADE_EXPOSURE_CAP_PCT`, `GLOBAL_MAX_OPEN_EXPOSURE_PCT`, `WINDOW_MAX_OPEN_EXPOSURE_PCT`, and `MINIMUM_TRADE_STAKE_USD` into those fields. Update `config/kill_switches.py` so `KillSwitchSettings` exposes the exact Phase 3 policy knobs `max_drawdown_pct: float = 0.20`, `cooldown_minutes: int = 60`, and `block_unaccepted_signals: bool = True`, and remove or clearly defer the unusable `max_loss_streak` setting from runtime logic because streak-based pauses are deferred to Phase 4. Extend `core/models.py` with immutable Phase 3 records: `PortfolioSnapshot(current_bankroll_usd, peak_bankroll_usd, open_exposure_usd, open_exposure_by_window, captured_at)`, `RiskDecisionStatus` with `ALLOWED` and `BLOCKED`, and `RiskDecisionRecord` carrying `signal_evaluation_id`, `market_id`, `window_key`, `decision_status`, `decision_reason`, `triggered_rule_codes`, `current_bankroll_usd`, `peak_bankroll_usd`, `open_exposure_usd`, `window_exposure_usd`, `proposed_stake_usd`, `allowed_stake_usd`, and `evidence`. Add `test_scan_settings_loads_risk_fields_from_env` to `tests/unit/test_settings.py` and assert the exact override keys round-trip.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_settings.py -q</automated>
  </verify>
  <done>The risk thresholds and record vocabulary exist as typed shared contracts instead of placeholder strings or loose dicts, and settings overrides prove the Phase 3 config surface is real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Kelly sizing and exposure-policy evaluation</name>
  <read_first>
    - core/kelly_engine.py
    - core/risk_manager.py
    - core/models.py
    - config/settings.py
    - config/kill_switches.py
    - tests/unit/test_kelly_engine.py
    - tests/unit/test_risk_manager.py
    - .planning/phases/03-risk-and-portfolio-controls/03-PATTERNS.md
  </read_first>
  <files>core/kelly_engine.py, core/risk_manager.py</files>
  <acceptance_criteria>
    - `rg -n "class KellyEngine" core/kelly_engine.py` returns one match.
    - `rg -n "class KellySizingDecision" core/kelly_engine.py` returns one match.
    - `rg -n "calculate_position_size\\(" core/kelly_engine.py` returns one match.
    - `rg -n "class RiskManager" core/risk_manager.py` returns one match.
    - `rg -n "evaluate_position_size\\(" core/risk_manager.py` returns one match.
    - `rg -n "per_trade_cap_exceeded|window_exposure_cap_exceeded|global_exposure_cap_exceeded|stake_below_minimum" core/kelly_engine.py core/risk_manager.py` returns matches for all four strings.
    - `uv run --extra dev pytest tests/unit/test_kelly_engine.py tests/unit/test_risk_manager.py -q` exits 0.
  </acceptance_criteria>
  <action>Replace the placeholders in `core/kelly_engine.py` and `core/risk_manager.py` with real Phase 3 logic. In `core/kelly_engine.py`, add `KellyEngine` plus a rich `KellySizingDecision` dataclass. The public entrypoint must be `calculate_position_size(derived_no_probability: float, no_price: float, bankroll_usd: float) -> KellySizingDecision`. Implement the exact sequence: clamp `derived_no_probability` into a safe probability range, subtract `probability_haircut_pct` to produce `adjusted_no_probability`, compute the binary-outcome Kelly fraction `f = p - q / b` using `b = (1 / no_price) - 1`, multiply by `kelly_fraction`, floor negative fractions to zero, convert to `proposed_stake_usd`, cap by `per_trade_exposure_cap_pct * bankroll_usd`, and block with `stake_below_minimum` if the capped stake is below `minimum_trade_stake_usd`. Record every clamp in `clamp_reasons`. In `core/risk_manager.py`, add `RiskManager` with `evaluate_position_size(signal_evaluation: SignalEvaluationRecord, portfolio: PortfolioSnapshot, sizing: KellySizingDecision, window_key: str) -> RiskDecisionRecord`. It must compute remaining headroom from `global_max_open_exposure_pct` and `window_max_open_exposure_pct`, cap the allowed stake by those remaining headrooms, emit explicit rule codes `global_exposure_cap_exceeded` or `window_exposure_cap_exceeded` when headroom is zero or negative, and return `RiskDecisionStatus.ALLOWED` only when a positive `allowed_stake_usd` survives all caps. Preserve `current_bankroll_usd` for stake and exposure math and `peak_bankroll_usd` only as a carried field for later drawdown use.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/unit/test_kelly_engine.py tests/unit/test_risk_manager.py -q</automated>
  </verify>
  <done>Phase 3 no longer has placeholder sizing and exposure modules; the repo has deterministic Kelly math and policy evaluation with explicit clamp reasons and passing unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal evaluation -> Kelly engine | Derived probabilities and market prices from Phase 2 become bankroll-sized stake proposals |
| portfolio snapshot -> risk manager | Runtime bankroll and open-exposure state influences whether a trade can be allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `core/kelly_engine.py` | mitigate | Clamp invalid probabilities, floor negative Kelly fractions, and record clamp reasons explicitly |
| T-03-02 | D | `core/risk_manager.py` | mitigate | Hard-block when exposure headroom is exhausted instead of allowing oversize stakes through |
| T-03-03 | I | `core/risk_manager.py` | accept | Portfolio inputs are local and caller-supplied in Phase 3; persistence and cross-run state integrity are covered in 03-02 |
</threat_model>

<verification>
Run `uv run --extra dev pytest tests/unit/test_kelly_engine.py tests/unit/test_risk_manager.py -q`.
Run `uv run --extra dev pytest tests/unit -q` after the plan completes to confirm the new risk config and policy code do not break prior unit coverage.
</verification>

<success_criteria>
The repo exposes real Kelly sizing and exposure-policy modules, risk thresholds are typed and configurable, and deterministic unit coverage proves stakes are haircut-aware, capped, and explicitly blocked when too small or over-exposed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-risk-and-portfolio-controls/03-risk-and-portfolio-controls-01-SUMMARY.md`
</output>
