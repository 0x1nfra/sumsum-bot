---
phase: 03-risk-and-portfolio-controls
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - core/models.py
  - core/storage.py
  - core/risk_manager.py
  - strategies/weather/signal_engine.py
  - tests/integration/test_risk_storage.py
  - tests/integration/test_signal_risk_gate.py
autonomous: true
requirements: [RISK-01, RISK-02, RISK-03]
must_haves:
  truths:
    - "Only accepted Phase 2 signal evaluations are eligible for a tradable risk decision; all unsafe upstream states fail closed."
    - "Drawdown halt and cooldown rules can block otherwise valid trades before sizing approval is returned."
    - "Blocked and allowed risk decisions are persisted append-only with rich evidence and linked signal references."
    - "Operator-reviewable risk records preserve proposed stake, allowed stake, bankroll state, exposure snapshot, and triggered rule codes."
  artifacts:
    - path: "core/storage.py"
      provides: "Append-only risk decision persistence and list/query methods"
    - path: "core/risk_manager.py"
      provides: "Kill-switch evaluation and risk-decision orchestration over accepted signal evaluations"
    - path: "tests/integration/test_signal_risk_gate.py"
      provides: "End-to-end verification of signal-to-risk handoff and persistence"
  key_links:
    - from: "strategies/weather/signal_engine.py"
      to: "core/risk_manager.py"
      via: "accepted SignalEvaluationRecord becomes a risk decision input"
      pattern: "SignalEvaluationRecord|RiskDecisionRecord"
    - from: "core/risk_manager.py"
      to: "core/storage.py"
      via: "append-only risk decision rows"
      pattern: "persist_risk_decisions|list_risk_decisions"
---

<objective>
Add the Phase 3 hard gates around accepted weather signals: drawdown and cooldown blocks, append-only risk-decision persistence, and an integration path that records why a signal was allowed or blocked before any paper trade lifecycle exists.

Purpose: finish RISK-01, satisfy RISK-02 and RISK-03, and leave Phase 4 with a reusable risk-decision audit trail instead of implicit in-memory policy.
Output: a non-placeholder kill-switch flow in `RiskManager`, append-only `risk_decisions` storage, and integration coverage proving accepted signals become persisted allowed or blocked decisions with explicit reasons.
</objective>

<execution_context>
@/Users/irfanmurad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-risk-and-portfolio-controls/03-CONTEXT.md
@.planning/phases/03-risk-and-portfolio-controls/03-RESEARCH.md
@.planning/phases/03-risk-and-portfolio-controls/03-VALIDATION.md
@.planning/phases/03-risk-and-portfolio-controls/03-PATTERNS.md
@config/settings.py
@config/kill_switches.py
@core/models.py
@core/storage.py
@core/risk_manager.py
@strategies/weather/signal_engine.py
@tests/integration/test_signal_storage.py
@tests/integration/test_noaa_signal_engine.py

<interfaces>
From core/models.py after 03-01:
```python
@dataclass(frozen=True)
class PortfolioSnapshot:
    current_bankroll_usd: float
    peak_bankroll_usd: float
    open_exposure_usd: float
    open_exposure_by_window: dict[str, float]
    captured_at: str

@dataclass(frozen=True)
class RiskDecisionRecord:
    signal_evaluation_id: int | None
    market_id: str
    window_key: str
    decision_status: RiskDecisionStatus
    decision_reason: str
    triggered_rule_codes: tuple[str, ...]
    ...
```

From strategies/weather/signal_engine.py:
```python
class SignalEngine:
    def evaluate_candidates(
        self,
        candidates: tuple[CandidateRecord, ...] | list[CandidateRecord] | None = None,
    ) -> SignalEngineResult:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration coverage for append-only risk storage and signal-to-risk gating</name>
  <read_first>
    - core/storage.py
    - core/risk_manager.py
    - core/models.py
    - strategies/weather/signal_engine.py
    - tests/integration/test_signal_storage.py
    - tests/integration/test_noaa_signal_engine.py
    - .planning/phases/03-risk-and-portfolio-controls/03-VALIDATION.md
  </read_first>
  <files>tests/integration/test_risk_storage.py, tests/integration/test_signal_risk_gate.py</files>
  <acceptance_criteria>
    - `rg -n "test_risk_storage_appends_allowed_and_blocked_decisions" tests/integration/test_risk_storage.py` returns one match.
    - `rg -n "test_signal_risk_gate_persists_blocked_decision_with_drawdown_reason" tests/integration/test_signal_risk_gate.py` returns one match.
    - `rg -n "test_signal_risk_gate_persists_allowed_decision_with_linked_signal_reference" tests/integration/test_signal_risk_gate.py` returns one match.
    - `rg -n "drawdown_halt_active|cooldown_active|signal_not_accepted|edge_threshold_passed" tests/integration/test_risk_storage.py tests/integration/test_signal_risk_gate.py` returns matches for all four strings.
  </acceptance_criteria>
  <action>Create `tests/integration/test_risk_storage.py` and `tests/integration/test_signal_risk_gate.py` following the repo's existing real-storage integration-test style. `test_risk_storage.py` must insert one allowed and one blocked `RiskDecisionRecord`, assert a new `risk_decisions` table exists, and verify direct SQL row values plus repository round-trip behavior. `test_signal_risk_gate.py` must use real `CandidateStorage` plus a real `RiskManager` and explicit stubbed signal inputs to verify two paths: an accepted Phase 2-style signal that becomes an allowed risk decision with a linked `signal_evaluation_id`, and a drawdown-triggered block that persists `drawdown_halt_active` plus the proposed-vs-allowed stake evidence. Include an explicit `cooldown_active` case or fixture in the suite so Task 2 can lock that rule without inventing later runtime dependencies.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_risk_storage.py tests/integration/test_signal_risk_gate.py -q</automated>
  </verify>
  <done>Red-first integration coverage exists for append-only risk storage and the end-to-end signal-to-risk decision path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement kill switches and append-only risk decision persistence</name>
  <read_first>
    - core/storage.py
    - core/risk_manager.py
    - core/models.py
    - config/kill_switches.py
    - tests/integration/test_risk_storage.py
    - tests/integration/test_signal_risk_gate.py
    - .planning/phases/03-risk-and-portfolio-controls/03-CONTEXT.md
  </read_first>
  <files>core/models.py, core/storage.py, core/risk_manager.py</files>
  <acceptance_criteria>
    - `rg -n "CREATE TABLE IF NOT EXISTS risk_decisions" core/storage.py` returns one match.
    - `rg -n "persist_risk_decisions\\(" core/storage.py` returns one match.
    - `rg -n "list_risk_decisions\\(" core/storage.py` returns one match.
    - `rg -n "drawdown_halt_active|cooldown_active|signal_not_accepted" core/risk_manager.py` returns matches for all three strings.
    - `rg -n "signal_evaluation_id|triggered_rule_codes|allowed_stake_usd|proposed_stake_usd" core/storage.py core/models.py` returns matches for all four strings.
    - `uv run --extra dev pytest tests/integration/test_risk_storage.py tests/integration/test_signal_risk_gate.py -q` exits 0.
  </acceptance_criteria>
  <action>Extend `core/storage.py` with a new append-only `risk_decisions` table and repository APIs `persist_risk_decisions(source: str, decisions: Iterable[RiskDecisionRecord]) -> int` and `list_risk_decisions(market_id: str | None = None) -> list[RiskDecisionRecord]`. The table must persist these exact fields: `signal_evaluation_id`, `market_id`, `window_key`, `decision_status`, `decision_reason`, `triggered_rule_codes_csv`, `current_bankroll_usd`, `peak_bankroll_usd`, `open_exposure_usd`, `window_exposure_usd`, `proposed_stake_usd`, `allowed_stake_usd`, `evidence_json`, and `evaluated_at`. Update `RiskDecisionRecord` if needed so storage round-trips those fields cleanly. In `core/risk_manager.py`, extend the class from 03-01 with kill-switch evaluation that runs before or alongside exposure caps: block with `signal_not_accepted` when the upstream `SignalEvaluationRecord.status` is not accepted; block with `drawdown_halt_active` when `(peak_bankroll_usd - current_bankroll_usd) / peak_bankroll_usd >= max_drawdown_pct`; block with `cooldown_active` when the caller-supplied cooldown state indicates the pause window has not expired; otherwise continue into the 03-01 sizing and exposure logic. The returned `RiskDecisionRecord.evidence` must include `signal_status`, `triggered_rule_codes`, `current_bankroll_usd`, `peak_bankroll_usd`, `open_exposure_usd`, `window_exposure_usd`, `proposed_stake_usd`, and `allowed_stake_usd`.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_risk_storage.py tests/integration/test_signal_risk_gate.py -q</automated>
  </verify>
  <done>The repo has a real append-only risk decision table and a non-placeholder kill-switch layer that explains every block with structured evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire accepted signal evaluations into persisted risk decisions</name>
  <read_first>
    - strategies/weather/signal_engine.py
    - core/risk_manager.py
    - core/storage.py
    - core/models.py
    - tests/integration/test_signal_risk_gate.py
    - tests/integration/test_noaa_signal_engine.py
    - .planning/phases/03-risk-and-portfolio-controls/03-RESEARCH.md
  </read_first>
  <files>strategies/weather/signal_engine.py, core/risk_manager.py, core/storage.py</files>
  <acceptance_criteria>
    - `rg -n "evaluate_risk_for_signal" strategies/weather/signal_engine.py core/risk_manager.py` returns at least one match.
    - `rg -n "persist_risk_decisions" strategies/weather/signal_engine.py core/storage.py` returns at least one match.
    - `rg -n "risk_decision_count|blocked|allowed" strategies/weather/signal_engine.py` returns matches for all three strings.
    - `uv run --extra dev pytest tests/integration/test_signal_risk_gate.py tests/integration/test_noaa_signal_engine.py -q` exits 0.
  </acceptance_criteria>
  <action>Extend the Phase 2 signal orchestration so Phase 3 exposes a real risk gate without prematurely building paper execution. Add a narrow integration path that takes accepted `SignalEvaluationRecord` outputs plus a caller-supplied `PortfolioSnapshot`, evaluates them through `RiskManager`, persists the resulting `RiskDecisionRecord` rows via `CandidateStorage.persist_risk_decisions`, and returns a result object that separates `allowed` and `blocked` decisions plus `risk_decision_count`. This integration must not re-run NOAA fetches or edge calculations, and it must not create paper-trade lifecycle state. Keep the existing `SignalEngine.evaluate_candidates()` behavior intact for Phase 2 tests; add a separate method or thin companion flow such as `evaluate_risk_for_signal(...)` rather than silently changing the meaning of accepted signal evaluations. Persist the linked signal reference so the blocked-decision audit trail can point back to the originating Phase 2 evaluation.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/integration/test_signal_risk_gate.py tests/integration/test_noaa_signal_engine.py -q</automated>
  </verify>
  <done>Phase 3 ends with a reusable risk gate around accepted weather signals: real kill switches, persisted decision rows, and an integration seam Phase 4 can call without duplicating policy logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted signal evaluations -> risk gate | Phase 2 outputs become tradability decisions and persisted audit records |
| risk gate -> SQLite persistence | Structured risk evidence crosses into durable local storage for later operator review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `core/risk_manager.py` | mitigate | Block non-accepted upstream signals and record the exact rule codes rather than allowing implicit fallthrough |
| T-03-02 | D | `core/risk_manager.py` | mitigate | Enforce drawdown halt and cooldown before approval so repeated unsafe trades cannot bypass the pause window |
| T-03-03 | I | `core/storage.py` | mitigate | Persist append-only risk rows with explicit fields and evidence JSON so earlier decisions are never overwritten or obscured |
</threat_model>

<verification>
Run `uv run --extra dev pytest tests/integration/test_risk_storage.py tests/integration/test_signal_risk_gate.py -q`.
Run `uv run --extra dev pytest tests/integration/test_signal_risk_gate.py tests/integration/test_noaa_signal_engine.py -q` after the integration path is added to confirm Phase 2 signal behavior remains intact.
</verification>

<success_criteria>
Accepted Phase 2 signals can be run through a reusable risk gate that persists explicit allowed or blocked decisions, drawdown and cooldown rules fail closed, and blocked trades remain inspectable through append-only storage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-risk-and-portfolio-controls/03-risk-and-portfolio-controls-02-SUMMARY.md`
</output>
